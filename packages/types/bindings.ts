// This file is auto-generated by tauri-specta. Do not edit manually.
// Run: bun run codegen
// @ts-nocheck
// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE } from "@tauri-apps/api/core";

/** Commands */
export const commands = {
	listWorkspaces: () => typedError<Workspace_Serialize[], VoleeoError>(__TAURI_INVOKE("list_workspaces")),
	createWorkspace: (name: string, encrypted: boolean | null) => typedError<Workspace_Serialize, VoleeoError>(__TAURI_INVOKE("create_workspace", { name, encrypted })),
	/**
	 *  Returns the 8×8 hex backup key for display to the user.
	 *  Only valid when `workspace.encrypted == true`.
	 */
	workspaceGetKeyDisplay: (workspaceId: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("workspace_get_key_display", { workspaceId })),
	/**
	 *  Enable encryption on an existing workspace: generate + store a key,
	 *  re-encrypt any existing plaintext secret, persist `encrypted = true` + a
	 *  key-check token, and return the display key for the backup card.
	 */
	workspaceEnableEncryption: (workspaceId: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("workspace_enable_encryption", { workspaceId })),
	/**
	 *  Import a backup key, replacing the current one. When the workspace has a
	 *  `keyCheck` token, decrypt it with the candidate and verify it matches the
	 *  workspace ID — catches typos before they lock the user out of their secrets.
	 */
	workspaceImportKey: (workspaceId: string, displayKey: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("workspace_import_key", { workspaceId, displayKey })),
	workspaceGetSettings: (workspaceId: string) => typedError<WorkspaceSettings_Serialize, VoleeoError>(__TAURI_INVOKE("workspace_get_settings", { workspaceId })),
	workspaceListSettings: () => typedError<{ [key in string]: WorkspaceSettings_Serialize }, VoleeoError>(__TAURI_INVOKE("workspace_list_settings")),
	workspaceSaveSettings: (workspaceId: string, settings: WorkspaceSettings_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("workspace_save_settings", { workspaceId, settings })),
	/**  Permanently delete a workspace and its encryption key (if any). */
	deleteWorkspace: (workspaceId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("delete_workspace", { workspaceId })),
	renameWorkspace: (workspaceId: string, name: string) => typedError<Workspace_Serialize, VoleeoError>(__TAURI_INVOKE("rename_workspace", { workspaceId, name })),
	/**
	 *  Absolute path of the workspace's data dir — the symlink target when synced,
	 *  else the internal app-data path.
	 */
	workspaceGetPath: (workspaceId: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("workspace_get_path", { workspaceId })),
	/**
	 *  Set (or clear) a workspace's sync directory. Setting one copies the files to
	 *  the chosen folder and replaces the internal dir with a symlink, so every
	 *  later write lands in the sync dir transparently.
	 */
	workspaceSetSyncDir: (workspaceId: string, syncDir: string | null) => typedError<Workspace_Serialize, VoleeoError>(__TAURI_INVOKE("workspace_set_sync_dir", { workspaceId, syncDir })),
	workspaceOpenFolder: (folderPath: string) => typedError<Workspace_Serialize, VoleeoError>(__TAURI_INVOKE("workspace_open_folder", { folderPath })),
	workspaceHasKey: (workspaceId: string) => typedError<boolean, VoleeoError>(__TAURI_INVOKE("workspace_has_key", { workspaceId })),
	/**
	 *  Encrypt a value with the workspace key → `enc:v1:…`. Backs the
	 *  `{{ encrypt(value) }}` template function. Requires encryption enabled.
	 */
	workspaceEncryptValue: (workspaceId: string, plaintext: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("workspace_encrypt_value", { workspaceId, plaintext })),
	/**
	 *  Decrypt a ciphertext produced by `workspace_encrypt_value` back to plaintext.
	 *  The workspace must have encryption enabled and a key available.
	 */
	workspaceDecryptValue: (workspaceId: string, ciphertext: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("workspace_decrypt_value", { workspaceId, ciphertext })),
	updateWorkspaceHeaders: (workspaceId: string, headers: RequestParameter[]) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_workspace_headers", { workspaceId, headers })),
	updateWorkspaceAuth: (workspaceId: string, auth: AuthConfig_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_workspace_auth", { workspaceId, auth })),
	updateWorkspaceDnsOverrides: (workspaceId: string, overrides: DnsOverride[]) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_workspace_dns_overrides", { workspaceId, overrides })),
	listRequests: (workspaceId: string) => typedError<HttpRequest_Serialize[], VoleeoError>(__TAURI_INVOKE("list_requests", { workspaceId })),
	listFolders: (workspaceId: string) => typedError<ApiFolder_Serialize[], VoleeoError>(__TAURI_INVOKE("list_folders", { workspaceId })),
	createRequest: (workspaceId: string, folderId: string | null, name: string, method: string, url: string) => typedError<HttpRequest_Serialize, VoleeoError>(__TAURI_INVOKE("create_request", { workspaceId, folderId, name, method, url })),
	createFolder: (workspaceId: string, folderId: string | null, name: string) => typedError<ApiFolder_Serialize, VoleeoError>(__TAURI_INVOKE("create_folder", { workspaceId, folderId, name })),
	duplicateRequest: (workspaceId: string, id: string) => typedError<HttpRequest_Serialize, VoleeoError>(__TAURI_INVOKE("duplicate_request", { workspaceId, id })),
	duplicateFolder: (workspaceId: string, id: string) => typedError<ApiFolder_Serialize, VoleeoError>(__TAURI_INVOKE("duplicate_folder", { workspaceId, id })),
	renameRequest: (workspaceId: string, id: string, name: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("rename_request", { workspaceId, id, name })),
	updateRequest: (workspaceId: string, id: string, method: string, url: string, parameters: RequestParameter[], headers: RequestParameter[], body: {
	kind: BodyKind,
	text?: string,
	fields?: BodyField_Deserialize[] | null,
	filePath?: string | null,
	contentType?: string | null,
	/**  GraphQL variables (JSON object string). Only meaningful for `Graphql`. */
	graphqlVariables?: string | null,
} | null, auth: AuthConfig_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_request", { workspaceId, id, method, url, parameters, headers, body, auth })),
	renameFolder: (workspaceId: string, id: string, name: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("rename_folder", { workspaceId, id, name })),
	updateFolder: (workspaceId: string, id: string, headers: RequestParameter[], auth: AuthConfig_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_folder", { workspaceId, id, headers, auth })),
	updateFolderColor: (workspaceId: string, id: string, color: string | null) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_folder_color", { workspaceId, id, color })),
	updateFolderVariables: (workspaceId: string, id: string, variables: EnvironmentVariable[]) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_folder_variables", { workspaceId, id, variables })),
	deleteRequest: (workspaceId: string, id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("delete_request", { workspaceId, id })),
	deleteFolder: (workspaceId: string, id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("delete_folder", { workspaceId, id })),
	moveItems: (workspaceId: string, updates: MoveItemUpdate[]) => typedError<null, VoleeoError>(__TAURI_INVOKE("move_items", { workspaceId, updates })),
	/**  Parse (auto-detecting the format when not given) and return the preview tree. */
	importPreview: (format: "open_api" | "swagger2" | "postman" | "insomnia" | "bruno" | "yaak" | null, content: string) => typedError<ImportPreview_Serialize, VoleeoError>(__TAURI_INVOKE("import_preview", { format, content })),
	/**
	 *  Read a user-picked collection file's text (dialog returns a path; there's no
	 *  fs plugin on the JS side). Off-runtime — `std::fs` blocks.
	 */
	importReadFile: (path: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("import_read_file", { path })),
	/**
	 *  Fetch a remote spec by URL so the UI can import an OpenAPI/Swagger document
	 *  straight from a link. Returns the raw text for `import_preview`.
	 */
	importFetchUrl: (url: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("import_fetch_url", { url })),
	/**  Parse, filter by `selected`, and persist into the target workspace. */
	importCommit: (format: ImportFormat, content: string, dest: ImportDest, selected: string[] | null) => typedError<ImportSummary, VoleeoError>(__TAURI_INVOKE("import_commit", { format, content, dest, selected })),
	/**
	 *  Import a native Voleeo Bundle (`voleeoBundle: "1.0"`). Recreates each workspace
	 *  in the bundle verbatim — folders, HTTP/WS/gRPC requests, environments, and
	 *  workspace auth/headers/DNS — as a fresh, unencrypted workspace. Bypasses the
	 *  IR entirely (already core types). Returns the last workspace so the UI can
	 *  open it.
	 */
	importVoleeo: (content: string, workspaceIds: string[]) => typedError<ImportSummary, VoleeoError>(__TAURI_INVOKE("import_voleeo", { content, workspaceIds })),
	importVoleeoPreview: (content: string) => typedError<VoleeoBundlePreview_Serialize, VoleeoError>(__TAURI_INVOKE("import_voleeo_preview", { content })),
	exportSummary: () => typedError<ExportTarget[], VoleeoError>(__TAURI_INVOKE("export_summary")),
	/**
	 *  The notes the real export would attach, computed up front so the window can
	 *  show them before the user commits. Skips decryption (warnings don't depend on
	 *  secret values) and writes nothing.
	 */
	exportPreview: (workspaceIds: string[], format: ExportFormat, includeEnvironments: boolean, includePrivate: boolean, exportProto: boolean) => typedError<string[], VoleeoError>(__TAURI_INVOKE("export_preview", { workspaceIds, format, includeEnvironments, includePrivate, exportProto })),
	exportWorkspaces: (workspaceIds: string[], format: ExportFormat, includeEnvironments: boolean, includePrivate: boolean, exportProto: boolean, exportAsyncapi: boolean, dest: string) => typedError<ExportOutcome, VoleeoError>(__TAURI_INVOKE("export_workspaces", { workspaceIds, format, includeEnvironments, includePrivate, exportProto, exportAsyncapi, dest })),
	sendRequest: (workspaceId: string, requestId: string, overrides: SendOverrides_Deserialize) => typedError<HttpResponse_Serialize, VoleeoError>(__TAURI_INVOKE("send_request", { workspaceId, requestId, overrides })),
	cancelRequest: (requestId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("cancel_request", { requestId })),
	/**
	 *  Sign a dynamic auth scheme (AWS SigV4, OAuth 1.0) over a resolved request and
	 *  return the headers/query it would add — so preview and "Copy as …" can show
	 *  the real signature without sending. `auth` must already be resolved (templates
	 *  expanded, secrets decrypted); static/no auth yields empty lists. Pure, so no
	 *  app state is touched.
	 */
	signAuthHeaders: (auth: AuthConfig_Deserialize, method: string, url: string, body: {
	kind: BodyKind,
	text?: string,
	fields?: BodyField_Deserialize[] | null,
	filePath?: string | null,
	contentType?: string | null,
	/**  GraphQL variables (JSON object string). Only meaningful for `Graphql`. */
	graphqlVariables?: string | null,
} | null) => typedError<SignedAuthParts, VoleeoError>(__TAURI_INVOKE("sign_auth_headers", { auth, method, url, body })),
	oauth2TokenStatus: (workspaceId: string, auth: AuthConfig_Deserialize) => typedError<Oauth2TokenStatus, VoleeoError>(__TAURI_INVOKE("oauth2_token_status", { workspaceId, auth })),
	oauth2TokenDetails: (workspaceId: string, auth: AuthConfig_Deserialize) => typedError<{
	accessToken: string,
	tokenType: string,
	scope: string,
	expiresAt: number | null,
	refreshToken: string | null,
} | null, VoleeoError>(__TAURI_INVOKE("oauth2_token_details", { workspaceId, auth })),
	/**
	 *  Acquire a token per the grant type: a direct POST for client-credentials /
	 *  password, or the interactive browser + loopback flow for authorization-code.
	 */
	oauth2FetchToken: (workspaceId: string, auth: AuthConfig_Deserialize) => typedError<Oauth2TokenStatus, VoleeoError>(__TAURI_INVOKE("oauth2_fetch_token", { workspaceId, auth })),
	oauth2ClearToken: (workspaceId: string, auth: AuthConfig_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("oauth2_clear_token", { workspaceId, auth })),
	/**
	 *  Send-path: return a valid access token, refreshing or (for non-interactive
	 *  grants) fetching as needed. Authorization-code with no cached token errors —
	 *  the user must click Get Token first. Delegates to `voleeo_oauth::ensure_token`
	 *  so the Tauri UI and the MCP send path share one cache + acquisition logic.
	 */
	oauth2EnsureToken: (workspaceId: string, auth: AuthConfig_Deserialize) => typedError<string, VoleeoError>(__TAURI_INVOKE("oauth2_ensure_token", { workspaceId, auth })),
	graphqlIntrospect: (workspaceId: string, requestId: string, environmentId: string | null, query: string) => typedError<HttpResponse_Serialize, VoleeoError>(__TAURI_INVOKE("graphql_introspect", { workspaceId, requestId, environmentId, query })),
	listWsConnections: (workspaceId: string) => typedError<WsConnection_Serialize[], VoleeoError>(__TAURI_INVOKE("list_ws_connections", { workspaceId })),
	getWsConnection: (workspaceId: string, id: string) => typedError<WsConnection_Serialize, VoleeoError>(__TAURI_INVOKE("get_ws_connection", { workspaceId, id })),
	createWsConnection: (workspaceId: string, folderId: string | null, name: string, url: string) => typedError<WsConnection_Serialize, VoleeoError>(__TAURI_INVOKE("create_ws_connection", { workspaceId, folderId, name, url })),
	duplicateWsConnection: (workspaceId: string, id: string) => typedError<WsConnection_Serialize, VoleeoError>(__TAURI_INVOKE("duplicate_ws_connection", { workspaceId, id })),
	renameWsConnection: (workspaceId: string, id: string, name: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("rename_ws_connection", { workspaceId, id, name })),
	updateWsConnection: (workspaceId: string, id: string, url: string, parameters: RequestParameter[], headers: RequestParameter[], auth: AuthConfig_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_ws_connection", { workspaceId, id, url, parameters, headers, auth })),
	deleteWsConnection: (workspaceId: string, id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("delete_ws_connection", { workspaceId, id })),
	wsUpdatePosition: (workspaceId: string, id: string, folderId: string | null, order: number | null) => typedError<null, VoleeoError>(__TAURI_INVOKE("ws_update_position", { workspaceId, id, folderId, order })),
	/**
	 *  Resolves env/folder vars + auth backend-side; `auth_override` short-circuits
	 *  the stored auth (frontend uses it for already-walked `Inherit`).
	 */
	wsConnect: (workspaceId: string, connectionId: string, environmentId: string | null, authOverride: ({ kind: "none" }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  Request-only, resolved at send time. `from` picks the scope: nearest
 *  ancestor folder with an auth (default), or the workspace.
 */
({ kind: "inherit"; from?: InheritSource }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "bearer"; token: string; token_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "basic"; username: string; password: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "api_key"; key: string; value: string; location: ApiKeyLocation; value_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  AWS Signature Version 4. Signed at send time over the final request, so
 *  the resolved config travels to the executor rather than producing a
 *  header here. `session_token` is set only for temporary STS credentials.
 */
({ kind: "aws_sig_v4"; access_key: string; secret_key: string; secret_key_encrypted?: boolean; session_token?: string; session_token_encrypted?: boolean; region: string; service: string; enabled?: boolean }) & { audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; scope?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  OAuth 1.0 (RFC 5849), signed into the `Authorization` header at send time.
 *  `token`/`token_secret` empty = two-legged (consumer-only) flow.
 */
({ kind: "oauth1"; consumer_key: string; consumer_secret: string; consumer_secret_encrypted?: boolean; token?: string; token_secret?: string; token_secret_encrypted?: boolean; signature_method?: OAuth1Signature; realm?: string; 
/**  PEM-encoded RSA private key — only used by the `Rsa*` methods. */
private_key?: string; private_key_encrypted?: boolean; 
/**  Where the `oauth_*` params go (header vs query). */
params_location?: OAuth1Location; callback?: string; verifier?: string; timestamp?: string; nonce?: string; version?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; password?: never; password_encrypted?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; state?: never; token_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; workstation?: never } | 
/**
 *  OAuth 2.0 (RFC 6749). Resolves to a `Bearer` header from a machine-local
 *  token cache at send time — `client_secret`/`password` are the only at-rest
 *  secrets; the token itself never lives in the synced config.
 */
({ kind: "oauth2"; grant_type: OAuth2Grant; auth_url?: string; token_url: string; client_id: string; client_secret?: string; client_secret_encrypted?: boolean; scope?: string; audience?: string; client_auth?: OAuth2ClientAuth; use_pkce?: boolean; code_challenge_method?: OAuth2PkceMethod; code_verifier?: string; redirect_uri?: string; state?: string; username?: string; password?: string; password_encrypted?: boolean; use_external_browser?: boolean; enabled?: boolean }) & { access_key?: never; callback?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; region?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  HTTP Digest access authentication (RFC 7616). Challenge-response: the
 *  realm/nonce/algorithm/qop come from the server's `401`, so only the
 *  credentials are stored. Applied by the executor's challenge-retry loop.
 */
({ kind: "digest"; username: string; password?: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  NTLM (NTLMv2). Authenticates a *connection*, so the executor runs the
 *  Type1→Type2→Type3 handshake over a dedicated single connection. HTTP-only.
 */
({ kind: "ntlm"; username: string; password?: string; password_encrypted?: boolean; domain?: string; workstation?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never } | null) => typedError<null, VoleeoError>(__TAURI_INVOKE("ws_connect", { workspaceId, connectionId, environmentId, authOverride })),
	/**
	 *  Send a message (optimistic): `{{ VAR }}` tokens in text payloads are resolved,
	 *  then the outbound row is emitted + persisted immediately and enqueued.
	 */
	wsSendMessage: (workspaceId: string, connectionId: string, kind: WsMessageKind, data: string, environmentId: string | null) => typedError<WsMessage, VoleeoError>(__TAURI_INVOKE("ws_send_message", { workspaceId, connectionId, kind, data, environmentId })),
	wsDisconnect: (workspaceId: string, connectionId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("ws_disconnect", { workspaceId, connectionId })),
	wsIsConnected: (connectionId: string) => typedError<boolean, VoleeoError>(__TAURI_INVOKE("ws_is_connected", { connectionId })),
	/**  The current (newest) session — used to hydrate the live transcript on mount. */
	wsGetTranscript: (workspaceId: string, connectionId: string) => typedError<StoredWsSession, VoleeoError>(__TAURI_INVOKE("ws_get_transcript", { workspaceId, connectionId })),
	/**  Session history (newest first) for the connection's history picker. */
	wsListSessions: (workspaceId: string, connectionId: string) => typedError<StoredWsSessionSummary[], VoleeoError>(__TAURI_INVOKE("ws_list_sessions", { workspaceId, connectionId })),
	/**  A single past session's full transcript + lifecycle events. */
	wsGetSession: (workspaceId: string, connectionId: string, sessionId: string) => typedError<StoredWsSession, VoleeoError>(__TAURI_INVOKE("ws_get_session", { workspaceId, connectionId, sessionId })),
	wsClearTranscript: (workspaceId: string, connectionId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("ws_clear_transcript", { workspaceId, connectionId })),
	listGrpcRequests: (workspaceId: string) => typedError<GrpcRequest_Serialize[], VoleeoError>(__TAURI_INVOKE("list_grpc_requests", { workspaceId })),
	getGrpcRequest: (workspaceId: string, id: string) => typedError<GrpcRequest_Serialize, VoleeoError>(__TAURI_INVOKE("get_grpc_request", { workspaceId, id })),
	createGrpcRequest: (workspaceId: string, folderId: string | null, name: string, target: string) => typedError<GrpcRequest_Serialize, VoleeoError>(__TAURI_INVOKE("create_grpc_request", { workspaceId, folderId, name, target })),
	duplicateGrpcRequest: (workspaceId: string, id: string) => typedError<GrpcRequest_Serialize, VoleeoError>(__TAURI_INVOKE("duplicate_grpc_request", { workspaceId, id })),
	renameGrpcRequest: (workspaceId: string, id: string, name: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("rename_grpc_request", { workspaceId, id, name })),
	updateGrpcRequest: (workspaceId: string, id: string, update: GrpcRequestUpdate_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("update_grpc_request", { workspaceId, id, update })),
	deleteGrpcRequest: (workspaceId: string, id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("delete_grpc_request", { workspaceId, id })),
	grpcUpdatePosition: (workspaceId: string, id: string, folderId: string | null, order: number | null) => typedError<null, VoleeoError>(__TAURI_INVOKE("grpc_update_position", { workspaceId, id, folderId, order })),
	grpcListServices: (workspaceId: string, id: string, environmentId: string | null) => typedError<ProtoServiceInfo_Serialize[], VoleeoError>(__TAURI_INVOKE("grpc_list_services", { workspaceId, id, environmentId })),
	/**  Force a fresh descriptor build (reflection re-query / `.proto` recompile). */
	grpcRefreshDescriptors: (workspaceId: string, id: string, environmentId: string | null) => typedError<ProtoServiceInfo_Serialize[], VoleeoError>(__TAURI_INVOKE("grpc_refresh_descriptors", { workspaceId, id, environmentId })),
	/**  The full method info (input schema + RPC kind) the form renders from. */
	grpcDescribeMethod: (workspaceId: string, id: string, service: string, method: string, environmentId: string | null) => typedError<ProtoMethodInfo_Serialize, VoleeoError>(__TAURI_INVOKE("grpc_describe_method", { workspaceId, id, service, method, environmentId })),
	/**
	 *  Schema for an arbitrary message by full name — backs lazy `MessageRef`
	 *  expansion in the form (e.g. recursive or deeply-nested types).
	 */
	grpcDescribeMessage: (workspaceId: string, id: string, message: string, environmentId: string | null) => typedError<ProtoMessageSchema_Serialize, VoleeoError>(__TAURI_INVOKE("grpc_describe_message", { workspaceId, id, message, environmentId })),
	/**
	 *  `auth_override` short-circuits stored auth (frontend resolved `Inherit`);
	 *  `message_override` swaps the stored protobuf-JSON payload (form draft).
	 */
	grpcCall: (workspaceId: string, id: string, environmentId: string | null, authOverride: ({ kind: "none" }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  Request-only, resolved at send time. `from` picks the scope: nearest
 *  ancestor folder with an auth (default), or the workspace.
 */
({ kind: "inherit"; from?: InheritSource }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "bearer"; token: string; token_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "basic"; username: string; password: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "api_key"; key: string; value: string; location: ApiKeyLocation; value_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  AWS Signature Version 4. Signed at send time over the final request, so
 *  the resolved config travels to the executor rather than producing a
 *  header here. `session_token` is set only for temporary STS credentials.
 */
({ kind: "aws_sig_v4"; access_key: string; secret_key: string; secret_key_encrypted?: boolean; session_token?: string; session_token_encrypted?: boolean; region: string; service: string; enabled?: boolean }) & { audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; scope?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  OAuth 1.0 (RFC 5849), signed into the `Authorization` header at send time.
 *  `token`/`token_secret` empty = two-legged (consumer-only) flow.
 */
({ kind: "oauth1"; consumer_key: string; consumer_secret: string; consumer_secret_encrypted?: boolean; token?: string; token_secret?: string; token_secret_encrypted?: boolean; signature_method?: OAuth1Signature; realm?: string; 
/**  PEM-encoded RSA private key — only used by the `Rsa*` methods. */
private_key?: string; private_key_encrypted?: boolean; 
/**  Where the `oauth_*` params go (header vs query). */
params_location?: OAuth1Location; callback?: string; verifier?: string; timestamp?: string; nonce?: string; version?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; password?: never; password_encrypted?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; state?: never; token_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; workstation?: never } | 
/**
 *  OAuth 2.0 (RFC 6749). Resolves to a `Bearer` header from a machine-local
 *  token cache at send time — `client_secret`/`password` are the only at-rest
 *  secrets; the token itself never lives in the synced config.
 */
({ kind: "oauth2"; grant_type: OAuth2Grant; auth_url?: string; token_url: string; client_id: string; client_secret?: string; client_secret_encrypted?: boolean; scope?: string; audience?: string; client_auth?: OAuth2ClientAuth; use_pkce?: boolean; code_challenge_method?: OAuth2PkceMethod; code_verifier?: string; redirect_uri?: string; state?: string; username?: string; password?: string; password_encrypted?: boolean; use_external_browser?: boolean; enabled?: boolean }) & { access_key?: never; callback?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; region?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  HTTP Digest access authentication (RFC 7616). Challenge-response: the
 *  realm/nonce/algorithm/qop come from the server's `401`, so only the
 *  credentials are stored. Applied by the executor's challenge-retry loop.
 */
({ kind: "digest"; username: string; password?: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  NTLM (NTLMv2). Authenticates a *connection*, so the executor runs the
 *  Type1→Type2→Type3 handshake over a dedicated single connection. HTTP-only.
 */
({ kind: "ntlm"; username: string; password?: string; password_encrypted?: boolean; domain?: string; workstation?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never } | null, messageOverride: string | null) => typedError<GrpcResponse, VoleeoError>(__TAURI_INVOKE("grpc_call", { workspaceId, id, environmentId, authOverride, messageOverride })),
	grpcCancel: (requestId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("grpc_cancel", { requestId })),
	grpcListUnaryResponses: (workspaceId: string, requestId: string) => typedError<StoredGrpcResponseSummary[], VoleeoError>(__TAURI_INVOKE("grpc_list_unary_responses", { workspaceId, requestId })),
	grpcGetUnaryResponse: (workspaceId: string, requestId: string, responseId: string) => typedError<GrpcResponse, VoleeoError>(__TAURI_INVOKE("grpc_get_unary_response", { workspaceId, requestId, responseId })),
	grpcClearUnaryResponses: (workspaceId: string, requestId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("grpc_clear_unary_responses", { workspaceId, requestId })),
	/**
	 *  Open a streaming call. `message_override` swaps the stored first-message
	 *  payload (form draft); `auth_override` short-circuits stored `Inherit` auth.
	 */
	grpcStreamStart: (workspaceId: string, id: string, environmentId: string | null, authOverride: ({ kind: "none" }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  Request-only, resolved at send time. `from` picks the scope: nearest
 *  ancestor folder with an auth (default), or the workspace.
 */
({ kind: "inherit"; from?: InheritSource }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "bearer"; token: string; token_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "basic"; username: string; password: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "api_key"; key: string; value: string; location: ApiKeyLocation; value_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  AWS Signature Version 4. Signed at send time over the final request, so
 *  the resolved config travels to the executor rather than producing a
 *  header here. `session_token` is set only for temporary STS credentials.
 */
({ kind: "aws_sig_v4"; access_key: string; secret_key: string; secret_key_encrypted?: boolean; session_token?: string; session_token_encrypted?: boolean; region: string; service: string; enabled?: boolean }) & { audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; scope?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  OAuth 1.0 (RFC 5849), signed into the `Authorization` header at send time.
 *  `token`/`token_secret` empty = two-legged (consumer-only) flow.
 */
({ kind: "oauth1"; consumer_key: string; consumer_secret: string; consumer_secret_encrypted?: boolean; token?: string; token_secret?: string; token_secret_encrypted?: boolean; signature_method?: OAuth1Signature; realm?: string; 
/**  PEM-encoded RSA private key — only used by the `Rsa*` methods. */
private_key?: string; private_key_encrypted?: boolean; 
/**  Where the `oauth_*` params go (header vs query). */
params_location?: OAuth1Location; callback?: string; verifier?: string; timestamp?: string; nonce?: string; version?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; password?: never; password_encrypted?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; state?: never; token_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; workstation?: never } | 
/**
 *  OAuth 2.0 (RFC 6749). Resolves to a `Bearer` header from a machine-local
 *  token cache at send time — `client_secret`/`password` are the only at-rest
 *  secrets; the token itself never lives in the synced config.
 */
({ kind: "oauth2"; grant_type: OAuth2Grant; auth_url?: string; token_url: string; client_id: string; client_secret?: string; client_secret_encrypted?: boolean; scope?: string; audience?: string; client_auth?: OAuth2ClientAuth; use_pkce?: boolean; code_challenge_method?: OAuth2PkceMethod; code_verifier?: string; redirect_uri?: string; state?: string; username?: string; password?: string; password_encrypted?: boolean; use_external_browser?: boolean; enabled?: boolean }) & { access_key?: never; callback?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; region?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  HTTP Digest access authentication (RFC 7616). Challenge-response: the
 *  realm/nonce/algorithm/qop come from the server's `401`, so only the
 *  credentials are stored. Applied by the executor's challenge-retry loop.
 */
({ kind: "digest"; username: string; password?: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  NTLM (NTLMv2). Authenticates a *connection*, so the executor runs the
 *  Type1→Type2→Type3 handshake over a dedicated single connection. HTTP-only.
 */
({ kind: "ntlm"; username: string; password?: string; password_encrypted?: boolean; domain?: string; workstation?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never } | null, messageOverride: string | null) => typedError<null, VoleeoError>(__TAURI_INVOKE("grpc_stream_start", { workspaceId, id, environmentId, authOverride, messageOverride })),
	/**
	 *  Send a client→server message (client-streaming/bidi). `{{ VAR }}` tokens in
	 *  the JSON payload are resolved; the row is emitted + persisted optimistically.
	 */
	grpcStreamSend: (workspaceId: string, id: string, message: string, environmentId: string | null) => typedError<GrpcStreamMessage, VoleeoError>(__TAURI_INVOKE("grpc_stream_send", { workspaceId, id, message, environmentId })),
	grpcStreamCloseSend: (id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("grpc_stream_close_send", { id })),
	grpcStreamCancel: (workspaceId: string, id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("grpc_stream_cancel", { workspaceId, id })),
	grpcIsActive: (id: string) => typedError<boolean, VoleeoError>(__TAURI_INVOKE("grpc_is_active", { id })),
	grpcGetTranscript: (workspaceId: string, id: string) => typedError<StoredGrpcSession, VoleeoError>(__TAURI_INVOKE("grpc_get_transcript", { workspaceId, id })),
	grpcListSessions: (workspaceId: string, id: string) => typedError<StoredGrpcSessionSummary[], VoleeoError>(__TAURI_INVOKE("grpc_list_sessions", { workspaceId, id })),
	grpcGetSession: (workspaceId: string, id: string, sessionId: string) => typedError<StoredGrpcSession, VoleeoError>(__TAURI_INVOKE("grpc_get_session", { workspaceId, id, sessionId })),
	grpcClearTranscript: (workspaceId: string, id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("grpc_clear_transcript", { workspaceId, id })),
	responseList: (workspaceId: string, requestId: string) => typedError<StoredHttpResponseSummary[], VoleeoError>(__TAURI_INVOKE("response_list", { workspaceId, requestId })),
	responseGet: (workspaceId: string, requestId: string, responseId: string) => typedError<{
	id: string,
	workspaceId: string,
	requestId: string,
	recordedAt: string,
	response: HttpResponse_Serialize,
} | null, VoleeoError>(__TAURI_INVOKE("response_get", { workspaceId, requestId, responseId })),
	responseClear: (workspaceId: string, requestId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("response_clear", { workspaceId, requestId })),
	/**  A line window of a large (windowed) response body. Off-thread per rule 17. */
	responseBodyWindow: (workspaceId: string, responseId: string, startLine: number, count: number) => typedError<BodyWindow, VoleeoError>(__TAURI_INVOKE("response_body_window", { workspaceId, responseId, startLine, count })),
	/**  Search a windowed response body; returns capped `{line, col, len}` matches. */
	responseBodySearch: (workspaceId: string, responseId: string, query: string, opts: SearchOpts) => typedError<BodySearchResult, VoleeoError>(__TAURI_INVOKE("response_body_search", { workspaceId, responseId, query, opts })),
	/**
	 *  Apply a JSONPath query to a windowed body; returns a key to window/search the
	 *  filtered result (empty query clears it).
	 */
	responseBodyFilter: (workspaceId: string, responseId: string, query: string) => typedError<BodyFilterResult, VoleeoError>(__TAURI_INVOKE("response_body_filter", { workspaceId, responseId, query })),
	settingsGetMcp: () => typedError<McpSettings, VoleeoError>(__TAURI_INVOKE("settings_get_mcp")),
	settingsSetMcpEnabled: (enabled: boolean) => typedError<McpSettings, VoleeoError>(__TAURI_INVOKE("settings_set_mcp_enabled", { enabled })),
	settingsRegenerateMcpToken: () => typedError<string, VoleeoError>(__TAURI_INVOKE("settings_regenerate_mcp_token")),
	settingsGetCustomTitleBar: () => typedError<boolean, VoleeoError>(__TAURI_INVOKE("settings_get_custom_title_bar")),
	settingsSetCustomTitleBar: (enabled: boolean) => typedError<null, VoleeoError>(__TAURI_INVOKE("settings_set_custom_title_bar", { enabled })),
	settingsGetAutoUpdate: () => typedError<boolean, VoleeoError>(__TAURI_INVOKE("settings_get_auto_update")),
	settingsSetAutoUpdate: (enabled: boolean) => typedError<null, VoleeoError>(__TAURI_INVOKE("settings_set_auto_update", { enabled })),
	repositionWindowControls: () => typedError<null, VoleeoError>(__TAURI_INVOKE("reposition_window_controls")),
	themeGetActive: () => typedError<string, VoleeoError>(__TAURI_INVOKE("theme_get_active")),
	themeActivate: (id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("theme_activate", { id })),
	themeGetColorMode: () => typedError<string, VoleeoError>(__TAURI_INVOKE("theme_get_color_mode")),
	themeSetColorMode: (mode: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("theme_set_color_mode", { mode })),
	getAppInfo: () => typedError<AppInfo, VoleeoError>(__TAURI_INVOKE("get_app_info")),
	/**
	 *  Toggle the main window's native menu bar (Windows auto-hide menu, revealed
	 *  with Alt). No-op off Windows.
	 */
	toggleMainMenu: () => typedError<null, VoleeoError>(__TAURI_INVOKE("toggle_main_menu")),
	debugEntityInfo: (workspaceId: string, kind: string, id: string) => typedError<EntityDebugInfo, VoleeoError>(__TAURI_INVOKE("debug_entity_info", { workspaceId, kind, id })),
	/**  Retrieve a stored value by key. Returns `null` if the key does not exist. */
	pluginStoreGet: (pluginId: string, key: string) => typedError<string | null, VoleeoError>(__TAURI_INVOKE("plugin_store_get", { pluginId, key })),
	/**  Set a value. The value must be a JSON string (the frontend serialises it). */
	pluginStoreSet: (pluginId: string, key: string, value: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("plugin_store_set", { pluginId, key, value })),
	/**  Delete a key. Returns true if the key existed. */
	pluginStoreDelete: (pluginId: string, key: string) => typedError<boolean, VoleeoError>(__TAURI_INVOKE("plugin_store_delete", { pluginId, key })),
	envList: (workspaceId: string) => typedError<Environment[], VoleeoError>(__TAURI_INVOKE("env_list", { workspaceId })),
	envGet: (workspaceId: string, id: string) => typedError<{
	id: string,
	workspaceId: string,
	kind: EnvironmentKind,
	name: string,
	color: string,
	shared: boolean,
	variables: EnvironmentVariable[],
	createdAt: string,
	updatedAt: string,
} | null, VoleeoError>(__TAURI_INVOKE("env_get", { workspaceId, id })),
	envCreate: (workspaceId: string, name: string, color: string, shared: boolean) => typedError<Environment, VoleeoError>(__TAURI_INVOKE("env_create", { workspaceId, name, color, shared })),
	envUpdate: (env: Environment) => typedError<Environment, VoleeoError>(__TAURI_INVOKE("env_update", { env })),
	envDelete: (workspaceId: string, id: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("env_delete", { workspaceId, id })),
	cookiesListJars: (workspaceId: string) => typedError<CookieJar_Serialize[], VoleeoError>(__TAURI_INVOKE("cookies_list_jars", { workspaceId })),
	cookiesCreateJar: (workspaceId: string, name: string) => typedError<CookieJar_Serialize, VoleeoError>(__TAURI_INVOKE("cookies_create_jar", { workspaceId, name })),
	cookiesRenameJar: (workspaceId: string, jarId: string, name: string) => typedError<CookieJar_Serialize, VoleeoError>(__TAURI_INVOKE("cookies_rename_jar", { workspaceId, jarId, name })),
	cookiesDeleteJar: (workspaceId: string, jarId: string) => typedError<string | null, VoleeoError>(__TAURI_INVOKE("cookies_delete_jar", { workspaceId, jarId })),
	cookiesSetActiveJar: (workspaceId: string, jarId: string | null) => typedError<null, VoleeoError>(__TAURI_INVOKE("cookies_set_active_jar", { workspaceId, jarId })),
	/**
	 *  The active cookie jar id for a workspace, falling back to the default jar.
	 *  Lets the frontend hydrate its selection on load without a synced field.
	 */
	cookiesGetActiveJar: (workspaceId: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("cookies_get_active_jar", { workspaceId })),
	cookiesSaveCookie: (workspaceId: string, jarId: string, cookie: StoredCookie_Deserialize) => typedError<StoredCookie_Serialize, VoleeoError>(__TAURI_INVOKE("cookies_save_cookie", { workspaceId, jarId, cookie })),
	cookiesDeleteCookie: (workspaceId: string, jarId: string, cookieId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("cookies_delete_cookie", { workspaceId, jarId, cookieId })),
	cookiesClearJar: (workspaceId: string, jarId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("cookies_clear_jar", { workspaceId, jarId })),
	cookiesClearExpired: (workspaceId: string, jarId: string) => typedError<number, VoleeoError>(__TAURI_INVOKE("cookies_clear_expired", { workspaceId, jarId })),
	/**
	 *  Enumerate installed font families on the system. Names are deduplicated and
	 *  returned in alphabetical order. Runs the (potentially slow) catalog scan on
	 *  a blocking pool so the Tauri runtime stays responsive.
	 */
	listSystemFonts: () => typedError<string[], VoleeoError>(__TAURI_INVOKE("list_system_fonts")),
	gitRepoInfo: (workspaceId: string) => typedError<GitRepoInfo, VoleeoError>(__TAURI_INVOKE("git_repo_info", { workspaceId })),
	gitInit: (workspaceId: string) => typedError<GitRepoInfo, VoleeoError>(__TAURI_INVOKE("git_init", { workspaceId })),
	gitStatus: (workspaceId: string) => typedError<GitStatus_Serialize, VoleeoError>(__TAURI_INVOKE("git_status", { workspaceId })),
	/**
	 *  All pending changes as decrypted entity snapshots (HEAD `old` vs working
	 *  `new`). The frontend turns these into the friendly field-level review.
	 */
	gitChanges: (workspaceId: string) => typedError<GitEntityChange_Serialize[], VoleeoError>(__TAURI_INVOKE("git_changes", { workspaceId })),
	gitEntityDiff: (workspaceId: string, path: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("git_entity_diff", { workspaceId, path })),
	gitConflictDiff: (workspaceId: string, path: string) => typedError<string, VoleeoError>(__TAURI_INVOKE("git_conflict_diff", { workspaceId, path })),
	gitStage: (workspaceId: string, paths: string[]) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_stage", { workspaceId, paths })),
	gitStageAll: (workspaceId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_stage_all", { workspaceId })),
	gitUnstage: (workspaceId: string, paths: string[]) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_unstage", { workspaceId, paths })),
	gitUnstageAll: (workspaceId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_unstage_all", { workspaceId })),
	gitDiscard: (workspaceId: string, paths: string[]) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_discard", { workspaceId, paths })),
	gitCommit: (workspaceId: string, message: string, authorName: string | null, authorEmail: string | null) => typedError<GitCommit, VoleeoError>(__TAURI_INVOKE("git_commit", { workspaceId, message, authorName, authorEmail })),
	gitRemotes: (workspaceId: string) => typedError<GitRemoteInfo[], VoleeoError>(__TAURI_INVOKE("git_remotes", { workspaceId })),
	gitSetRemote: (workspaceId: string, name: string, url: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_set_remote", { workspaceId, name, url })),
	gitSetUpstream: (workspaceId: string, remote: string, branch: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_set_upstream", { workspaceId, remote, branch })),
	gitFetch: (workspaceId: string) => typedError<GitRepoInfo, VoleeoError>(__TAURI_INVOKE("git_fetch", { workspaceId })),
	gitPull: (workspaceId: string) => typedError<GitMergeResult, VoleeoError>(__TAURI_INVOKE("git_pull", { workspaceId })),
	gitPush: (workspaceId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_push", { workspaceId })),
	gitBranches: (workspaceId: string) => typedError<GitBranch[], VoleeoError>(__TAURI_INVOKE("git_branches", { workspaceId })),
	gitCheckout: (workspaceId: string, branch: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_checkout", { workspaceId, branch })),
	gitCreateBranch: (workspaceId: string, name: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_create_branch", { workspaceId, name })),
	gitRenameBranch: (workspaceId: string, oldName: string, newName: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_rename_branch", { workspaceId, oldName, newName })),
	/**  Clone `url` into `dest_parent/<repo-name>` and adopt it as a workspace. */
	gitCloneWorkspace: (url: string, destParent: string, username: string | null, password: string | null) => typedError<Workspace_Serialize, VoleeoError>(__TAURI_INVOKE("git_clone_workspace", { url, destParent, username, password })),
	gitSetCredentials: (workspaceId: string, username: string, password: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_set_credentials", { workspaceId, username, password })),
	gitClearCredentials: (workspaceId: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_clear_credentials", { workspaceId })),
	/**
	 *  The stored HTTPS username (never the token), so the settings form can show
	 *  whether credentials are configured.
	 */
	gitCredentialsUser: (workspaceId: string) => typedError<string | null, VoleeoError>(__TAURI_INVOKE("git_credentials_user", { workspaceId })),
	gitSetIdentity: (workspaceId: string, name: string, email: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_set_identity", { workspaceId, name, email })),
	gitGetIdentity: (workspaceId: string) => typedError<{
	name: string,
	email: string,
} | null, VoleeoError>(__TAURI_INVOKE("git_get_identity", { workspaceId })),
	/**  Each conflicted entity's three sides, decrypted, for the friendly chooser. */
	gitEntityConflicts: (workspaceId: string) => typedError<GitEntityConflict_Serialize[], VoleeoError>(__TAURI_INVOKE("git_entity_conflicts", { workspaceId })),
	/**
	 *  Write a user-merged entity back to its file (re-encrypting at rest when the
	 *  workspace is encrypted) and stage it, clearing the conflict in the index.
	 */
	gitResolveEntity: (workspaceId: string, path: string, entity: GitEntity_Deserialize) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_resolve_entity", { workspaceId, path, entity })),
	/**
	 *  Resolve a delete/modify conflict by accepting the deletion — the user chose
	 *  the side that removed the entity.
	 */
	gitResolveDelete: (workspaceId: string, path: string) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_resolve_delete", { workspaceId, path })),
	gitFinishMerge: (workspaceId: string, message: string, authorName: string | null, authorEmail: string | null) => typedError<GitCommit, VoleeoError>(__TAURI_INVOKE("git_finish_merge", { workspaceId, message, authorName, authorEmail })),
	gitLog: (workspaceId: string, limit: number) => typedError<GitCommit[], VoleeoError>(__TAURI_INVOKE("git_log", { workspaceId, limit })),
	gitLogForPath: (workspaceId: string, path: string, limit: number) => typedError<GitCommit[], VoleeoError>(__TAURI_INVOKE("git_log_for_path", { workspaceId, path, limit })),
	/**
	 *  The decrypted entity changes a single commit introduced (vs its parent) —
	 *  powers the read-only history viewer.
	 */
	gitCommitChanges: (workspaceId: string, commitId: string) => typedError<GitEntityChange_Serialize[], VoleeoError>(__TAURI_INVOKE("git_commit_changes", { workspaceId, commitId })),
	/**
	 *  Revert a commit by writing the pre-commit version of its files into the
	 *  working tree (unstaged) for the user to review and publish. `path` scopes the
	 *  revert to a single entity (per-file history); `None` reverts the whole commit.
	 */
	gitRevertCommit: (workspaceId: string, commitId: string, path: string | null) => typedError<null, VoleeoError>(__TAURI_INVOKE("git_revert_commit", { workspaceId, commitId, path })),
};

/* Types */
export type ApiFolder = ApiFolder_Serialize | ApiFolder_Deserialize;

export type ApiFolder_Deserialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	name: string,
	/**  Applied to requests in this folder + subfolders; nearest scope wins. */
	headers?: RequestParameter[],
	/**  Folder-level auth. Requests can opt in via `AuthConfig::Inherit`. */
	auth?: AuthConfig_Deserialize,
	/**
	 *  Variables for requests in this folder + subfolders (`{{ KEY }}`). Nearest
	 *  folder wins over ancestors, folders win over envs. Ciphertext at rest.
	 */
	variables?: EnvironmentVariable[],
	/**  Accent color (hex `#rrggbb`) for the folder icon; `None` = theme default. */
	color?: string | null,
	order?: number | null,
	createdAt: string,
	updatedAt: string,
};

export type ApiFolder_Serialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	name: string,
	/**  Applied to requests in this folder + subfolders; nearest scope wins. */
	headers?: RequestParameter[],
	/**  Folder-level auth. Requests can opt in via `AuthConfig::Inherit`. */
	auth?: AuthConfig_Serialize,
	/**
	 *  Variables for requests in this folder + subfolders (`{{ KEY }}`). Nearest
	 *  folder wins over ancestors, folders win over envs. Ciphertext at rest.
	 */
	variables?: EnvironmentVariable[],
	/**  Accent color (hex `#rrggbb`) for the folder icon; `None` = theme default. */
	color?: string | null,
	order: number | null,
	createdAt: string,
	updatedAt: string,
};

export type ApiKeyLocation = "header" | "query";

export type AppInfo = {
	version: string,
	data_dir: string,
	log_dir: string,
	bridge_path: string,
};

export type AuthConfig = AuthConfig_Serialize | AuthConfig_Deserialize;

export type AuthConfig_Deserialize = ({ kind: "none" }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  Request-only, resolved at send time. `from` picks the scope: nearest
 *  ancestor folder with an auth (default), or the workspace.
 */
({ kind: "inherit"; from?: InheritSource }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "bearer"; token: string; token_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "basic"; username: string; password: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "api_key"; key: string; value: string; location: ApiKeyLocation; value_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  AWS Signature Version 4. Signed at send time over the final request, so
 *  the resolved config travels to the executor rather than producing a
 *  header here. `session_token` is set only for temporary STS credentials.
 */
({ kind: "aws_sig_v4"; access_key: string; secret_key: string; secret_key_encrypted?: boolean; session_token?: string; session_token_encrypted?: boolean; region: string; service: string; enabled?: boolean }) & { audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; scope?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  OAuth 1.0 (RFC 5849), signed into the `Authorization` header at send time.
 *  `token`/`token_secret` empty = two-legged (consumer-only) flow.
 */
({ kind: "oauth1"; consumer_key: string; consumer_secret: string; consumer_secret_encrypted?: boolean; token?: string; token_secret?: string; token_secret_encrypted?: boolean; signature_method?: OAuth1Signature; realm?: string; 
/**  PEM-encoded RSA private key — only used by the `Rsa*` methods. */
private_key?: string; private_key_encrypted?: boolean; 
/**  Where the `oauth_*` params go (header vs query). */
params_location?: OAuth1Location; callback?: string; verifier?: string; timestamp?: string; nonce?: string; version?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; password?: never; password_encrypted?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; state?: never; token_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; workstation?: never } | 
/**
 *  OAuth 2.0 (RFC 6749). Resolves to a `Bearer` header from a machine-local
 *  token cache at send time — `client_secret`/`password` are the only at-rest
 *  secrets; the token itself never lives in the synced config.
 */
({ kind: "oauth2"; grant_type: OAuth2Grant; auth_url?: string; token_url: string; client_id: string; client_secret?: string; client_secret_encrypted?: boolean; scope?: string; audience?: string; client_auth?: OAuth2ClientAuth; use_pkce?: boolean; code_challenge_method?: OAuth2PkceMethod; code_verifier?: string; redirect_uri?: string; state?: string; username?: string; password?: string; password_encrypted?: boolean; use_external_browser?: boolean; enabled?: boolean }) & { access_key?: never; callback?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; region?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  HTTP Digest access authentication (RFC 7616). Challenge-response: the
 *  realm/nonce/algorithm/qop come from the server's `401`, so only the
 *  credentials are stored. Applied by the executor's challenge-retry loop.
 */
({ kind: "digest"; username: string; password?: string; password_encrypted?: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  NTLM (NTLMv2). Authenticates a *connection*, so the executor runs the
 *  Type1→Type2→Type3 handshake over a dedicated single connection. HTTP-only.
 */
({ kind: "ntlm"; username: string; password?: string; password_encrypted?: boolean; domain?: string; workstation?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never };

export type AuthConfig_Serialize = ({ kind: "none" }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  Request-only, resolved at send time. `from` picks the scope: nearest
 *  ancestor folder with an auth (default), or the workspace.
 */
({ kind: "inherit"; from: InheritSource }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; enabled?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "bearer"; token: string; token_encrypted: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "basic"; username: string; password: string; password_encrypted: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | ({ kind: "api_key"; key: string; value: string; location: ApiKeyLocation; value_encrypted: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  AWS Signature Version 4. Signed at send time over the final request, so
 *  the resolved config travels to the executor rather than producing a
 *  header here. `session_token` is set only for temporary STS credentials.
 */
({ kind: "aws_sig_v4"; access_key: string; secret_key: string; secret_key_encrypted: boolean; session_token?: string; session_token_encrypted: boolean; region: string; service: string; enabled?: boolean }) & { audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; password?: never; password_encrypted?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; scope?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  OAuth 1.0 (RFC 5849), signed into the `Authorization` header at send time.
 *  `token`/`token_secret` empty = two-legged (consumer-only) flow.
 */
({ kind: "oauth1"; consumer_key: string; consumer_secret: string; consumer_secret_encrypted: boolean; token?: string; token_secret?: string; token_secret_encrypted: boolean; signature_method: OAuth1Signature; realm?: string; 
/**  PEM-encoded RSA private key — only used by the `Rsa*` methods. */
private_key?: string; private_key_encrypted: boolean; 
/**  Where the `oauth_*` params go (header vs query). */
params_location: OAuth1Location; callback?: string; verifier?: string; timestamp?: string; nonce?: string; version?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; password?: never; password_encrypted?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; state?: never; token_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; username?: never; value?: never; value_encrypted?: never; workstation?: never } | 
/**
 *  OAuth 2.0 (RFC 6749). Resolves to a `Bearer` header from a machine-local
 *  token cache at send time — `client_secret`/`password` are the only at-rest
 *  secrets; the token itself never lives in the synced config.
 */
({ kind: "oauth2"; grant_type: OAuth2Grant; auth_url?: string; token_url: string; client_id: string; client_secret?: string; client_secret_encrypted: boolean; scope?: string; audience?: string; client_auth: OAuth2ClientAuth; use_pkce?: boolean; code_challenge_method?: OAuth2PkceMethod; code_verifier?: string; redirect_uri?: string; state?: string; username?: string; password?: string; password_encrypted: boolean; use_external_browser?: boolean; enabled?: boolean }) & { access_key?: never; callback?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; region?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  HTTP Digest access authentication (RFC 7616). Challenge-response: the
 *  realm/nonce/algorithm/qop come from the server's `401`, so only the
 *  credentials are stored. Applied by the executor's challenge-retry loop.
 */
({ kind: "digest"; username: string; password?: string; password_encrypted: boolean; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; domain?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never; workstation?: never } | 
/**
 *  NTLM (NTLMv2). Authenticates a *connection*, so the executor runs the
 *  Type1→Type2→Type3 handshake over a dedicated single connection. HTTP-only.
 */
({ kind: "ntlm"; username: string; password?: string; password_encrypted: boolean; domain?: string; workstation?: string; enabled?: boolean }) & { access_key?: never; audience?: never; auth_url?: never; callback?: never; client_auth?: never; client_id?: never; client_secret?: never; client_secret_encrypted?: never; code_challenge_method?: never; code_verifier?: never; consumer_key?: never; consumer_secret?: never; consumer_secret_encrypted?: never; from?: never; grant_type?: never; key?: never; location?: never; nonce?: never; params_location?: never; private_key?: never; private_key_encrypted?: never; realm?: never; redirect_uri?: never; region?: never; scope?: never; secret_key?: never; secret_key_encrypted?: never; service?: never; session_token?: never; session_token_encrypted?: never; signature_method?: never; state?: never; timestamp?: never; token?: never; token_encrypted?: never; token_secret?: never; token_secret_encrypted?: never; token_url?: never; use_external_browser?: never; use_pkce?: never; value?: never; value_encrypted?: never; verifier?: never; version?: never };

/**
 *  One field of a form-urlencoded or multipart body. `is_file` (multipart only)
 *  flags a file attachment whose `value` is an absolute path read at send time.
 */
export type BodyField = BodyField_Serialize | BodyField_Deserialize;

/**
 *  One field of a form-urlencoded or multipart body. `is_file` (multipart only)
 *  flags a file attachment whose `value` is an absolute path read at send time.
 */
export type BodyField_Deserialize = {
	id: string,
	name: string,
	value: string,
	enabled: boolean,
	isFile?: boolean,
	contentType?: string | null,
};

/**
 *  One field of a form-urlencoded or multipart body. `is_file` (multipart only)
 *  flags a file attachment whose `value` is an absolute path read at send time.
 */
export type BodyField_Serialize = {
	id: string,
	name: string,
	value: string,
	enabled: boolean,
	isFile: boolean,
	contentType?: string | null,
};

export type BodyFilterResult = {
	/**  Key to pass to window/search to view the filtered result. */
	filterKey: string,
	lineCount: number,
	/**  Top-level matches (0 = the query matched nothing). */
	matchCount: number,
	/**  Set when the body isn't JSON or the JSONPath is invalid. */
	error: string | null,
};

export type BodyKind = "none" | "json" | "xml" | "text" | "html" | "form_url_encoded" | "multipart" | "binary" | "graphql";

export type BodyMatch = {
	line: number,
	/**  Character offset within the line (0-based). */
	col: number,
	len: number,
};

export type BodySearchResult = {
	matches: BodyMatch[],
	total: number,
	/**  True when more matches existed than the returned cap. */
	truncated: boolean,
};

/**  A line window plus the body's total line count (for the virtual viewport). */
export type BodyWindow = {
	lines: string[],
	totalLines: number,
};

export type BundleWorkspacePreview = BundleWorkspacePreview_Serialize | BundleWorkspacePreview_Deserialize;

export type BundleWorkspacePreview_Deserialize = {
	/**
	 *  The bundle's own workspace id — the selection key passed back to
	 *  `import_voleeo` (a fresh id is minted on import).
	 */
	id: string,
	name: string,
	encrypted: boolean,
	requestCount: number,
	environmentCount: number,
	/**  Folder/request tree in the shared `ImportNode` shape the import tree renders. */
	tree: ImportNode_Deserialize[],
};

export type BundleWorkspacePreview_Serialize = {
	/**
	 *  The bundle's own workspace id — the selection key passed back to
	 *  `import_voleeo` (a fresh id is minted on import).
	 */
	id: string,
	name: string,
	encrypted: boolean,
	requestCount: number,
	environmentCount: number,
	/**  Folder/request tree in the shared `ImportNode` shape the import tree renders. */
	tree: ImportNode_Serialize[],
};

export type CookieJar = CookieJar_Serialize | CookieJar_Deserialize;

export type CookieJar_Deserialize = {
	id: string,
	workspaceId: string,
	name: string,
	cookies?: StoredCookie_Deserialize[],
	createdAt: string,
	updatedAt: string,
};

export type CookieJar_Serialize = {
	id: string,
	workspaceId: string,
	name: string,
	cookies: StoredCookie_Serialize[],
	createdAt: string,
	updatedAt: string,
};

/**
 *  One DNS override row. `address` is parsed as `IpAddr` (v4 or v6) at apply
 *  time; storing it as a string keeps the YAML readable and lets us roundtrip
 *  invalid drafts the user is still editing without losing them.
 */
export type DnsOverride = {
	id: string,
	enabled: boolean,
	hostname: string,
	address: string,
};

export type EntityDebugInfo = {
	fileName: string,
	/**  Logical path under the app data dir — `workspaces/{id}` may be a symlink. */
	logicalPath: string,
	/**  Canonical path with symlinks resolved (where the bytes actually live). */
	resolvedPath: string | null,
	exists: boolean,
	/**  `f64` for JS-number / specta interop. */
	sizeBytes: number | null,
	modified: string | null,
	/**  Set when `workspaces/{id}` is a symlink to a user sync dir. */
	syncLinkTarget: string | null,
	/**  Machine-local response-history file (requests / gRPC only). */
	responseFile: string | null,
	/**
	 *  The YAML file's verbatim on-disk content (secrets stay `enc:v1:` at rest —
	 *  not decrypted). `None` when absent or larger than 1 MiB.
	 */
	fileContent: string | null,
};

export type Environment = {
	id: string,
	workspaceId: string,
	kind: EnvironmentKind,
	name: string,
	color: string,
	shared: boolean,
	variables: EnvironmentVariable[],
	createdAt: string,
	updatedAt: string,
};

export type EnvironmentKind = "personal" | "global";

export type EnvironmentVariable = {
	key: string,
	/**  Plaintext on the IPC wire; on disk this is ciphertext (`enc:v1:...`) when `encrypted` is true. */
	value: string,
	encrypted: boolean,
	/**  When false, skipped during interpolation. Pre-field YAML reads as enabled. */
	enabled?: boolean,
};

export type ExportFormat = 
/**  Native, lossless: one self-contained YAML re-importable into Voleeo. */
"voleeo" | 
/**  Portable Postman Collection v2.1 (+ companion files). */
"postman";

export type ExportOutcome = {
	/**  Absolute path(s) written. */
	paths: string[],
	warnings: string[],
};

/**  Per-workspace counts the Export picker renders. */
export type ExportTarget = {
	id: string,
	name: string,
	/**  HTTP + WebSocket + gRPC requests. */
	requests: number,
	/**  WebSocket connections (drive the AsyncAPI section). */
	wsCount: number,
	/**  gRPC requests (drive the .proto section). */
	grpcCount: number,
	sharedEnvs: number,
	privateEnvs: number,
	sharedSecrets: number,
	privateSecrets: number,
	/**
	 *  Inline `encrypt()` secret chips in non-env fields (URLs, params, headers,
	 *  bodies) — always exported, so always counted toward the warning.
	 */
	inlineSecrets: number,
};

export type GitBranch = {
	name: string,
	current: boolean,
};

/**  How a tracked file changed relative to HEAD / the index. */
export type GitChange = "added" | "modified" | "deleted" | "renamed" | "conflicted" | "untracked";

export type GitCommit = {
	id: string,
	shortId: string,
	summary: string,
	author: string,
	email: string,
	timestamp: number | null,
};

/**
 *  A parsed, decrypted workspace entity at one git revision. Exactly one field
 *  is `Some`, matching `kind`. Built by the command layer (it owns the crypto);
 *  `voleeo-git` only ever deals in the raw YAML text behind it.
 */
export type GitEntity = GitEntity_Serialize | GitEntity_Deserialize;

/**
 *  A single changed entity for the friendly "Review changes" screen. `old` is the
 *  committed (HEAD) snapshot, `new` the working-tree snapshot — both decrypted so
 *  ciphertext nonce churn never shows as a change. `None` on the side where the
 *  entity does not exist (added ⇒ no old, deleted ⇒ no new).
 */
export type GitEntityChange = GitEntityChange_Serialize | GitEntityChange_Deserialize;

/**
 *  A single changed entity for the friendly "Review changes" screen. `old` is the
 *  committed (HEAD) snapshot, `new` the working-tree snapshot — both decrypted so
 *  ciphertext nonce churn never shows as a change. `None` on the side where the
 *  entity does not exist (added ⇒ no old, deleted ⇒ no new).
 */
export type GitEntityChange_Deserialize = {
	path: string,
	nodeId: string | null,
	nodeKind: GitNodeKind,
	status: GitChange,
	old: GitEntity_Deserialize | null,
	new: GitEntity_Deserialize | null,
};

/**
 *  A single changed entity for the friendly "Review changes" screen. `old` is the
 *  committed (HEAD) snapshot, `new` the working-tree snapshot — both decrypted so
 *  ciphertext nonce churn never shows as a change. `None` on the side where the
 *  entity does not exist (added ⇒ no old, deleted ⇒ no new).
 */
export type GitEntityChange_Serialize = {
	path: string,
	nodeId: string | null,
	nodeKind: GitNodeKind,
	status: GitChange,
	old: GitEntity_Serialize | null,
	new: GitEntity_Serialize | null,
};

/**
 *  A conflicted entity's three sides, parsed + decrypted from the index stages.
 *  `None` where that stage is absent (e.g. delete/modify conflicts).
 */
export type GitEntityConflict = GitEntityConflict_Serialize | GitEntityConflict_Deserialize;

/**
 *  A conflicted entity's three sides, parsed + decrypted from the index stages.
 *  `None` where that stage is absent (e.g. delete/modify conflicts).
 */
export type GitEntityConflict_Deserialize = {
	path: string,
	nodeId: string | null,
	nodeKind: GitNodeKind,
	base: GitEntity_Deserialize | null,
	ours: GitEntity_Deserialize | null,
	theirs: GitEntity_Deserialize | null,
};

/**
 *  A conflicted entity's three sides, parsed + decrypted from the index stages.
 *  `None` where that stage is absent (e.g. delete/modify conflicts).
 */
export type GitEntityConflict_Serialize = {
	path: string,
	nodeId: string | null,
	nodeKind: GitNodeKind,
	base: GitEntity_Serialize | null,
	ours: GitEntity_Serialize | null,
	theirs: GitEntity_Serialize | null,
};

/**
 *  A parsed, decrypted workspace entity at one git revision. Exactly one field
 *  is `Some`, matching `kind`. Built by the command layer (it owns the crypto);
 *  `voleeo-git` only ever deals in the raw YAML text behind it.
 */
export type GitEntity_Deserialize = {
	kind: GitNodeKind,
	request?: HttpRequest_Deserialize | null,
	connection?: WsConnection_Deserialize | null,
	grpc?: GrpcRequest_Deserialize | null,
	folder?: ApiFolder_Deserialize | null,
	environment?: Environment | null,
	jar?: CookieJar_Deserialize | null,
	workspace?: Workspace_Deserialize | null,
};

/**
 *  A parsed, decrypted workspace entity at one git revision. Exactly one field
 *  is `Some`, matching `kind`. Built by the command layer (it owns the crypto);
 *  `voleeo-git` only ever deals in the raw YAML text behind it.
 */
export type GitEntity_Serialize = {
	kind: GitNodeKind,
	request?: HttpRequest_Serialize | null,
	connection?: WsConnection_Serialize | null,
	grpc?: GrpcRequest_Serialize | null,
	folder?: ApiFolder_Serialize | null,
	environment?: Environment | null,
	jar?: CookieJar_Serialize | null,
	workspace?: Workspace_Serialize | null,
};

/**  One changed file, already mapped back to its tree node when possible. */
export type GitFileChange = GitFileChange_Serialize | GitFileChange_Deserialize;

/**  One changed file, already mapped back to its tree node when possible. */
export type GitFileChange_Deserialize = {
	path: string,
	nodeId: string | null,
	nodeKind: GitNodeKind,
	change: GitChange,
	staged: boolean,
	/**
	 *  Parent folder id for a DELETED entity, recovered from its HEAD YAML (the
	 *  working file is gone, so the frontend can't resolve it from live state).
	 *  `None` for non-deletions, root-level items, and non-foldered kinds.
	 */
	parentId?: string | null,
};

/**  One changed file, already mapped back to its tree node when possible. */
export type GitFileChange_Serialize = {
	path: string,
	nodeId: string | null,
	nodeKind: GitNodeKind,
	change: GitChange,
	staged: boolean,
	/**
	 *  Parent folder id for a DELETED entity, recovered from its HEAD YAML (the
	 *  working file is gone, so the frontend can't resolve it from live state).
	 *  `None` for non-deletions, root-level items, and non-foldered kinds.
	 */
	parentId?: string | null,
};

export type GitIdentity = {
	name: string,
	email: string,
};

/**
 *  Outcome of a pull: either fast-forwarded cleanly or left conflicts to resolve.
 *  The conflicting entities are loaded separately via `git_entity_conflicts`.
 */
export type GitMergeResult = {
	fastForwarded: boolean,
	conflicted: boolean,
	upToDate: boolean,
};

/**  Which Voleeo entity a changed file maps to, derived from its filename. */
export type GitNodeKind = "workspace" | "folder" | "request" | "webSocket" | "grpc" | "jar" | "env" | "other";

export type GitRemoteInfo = {
	name: string,
	url: string,
};

/**  Snapshot of repo-level state for the Source Control header + init flow. */
export type GitRepoInfo = {
	isRepo: boolean,
	branch: string | null,
	detached: boolean,
	hasRemote: boolean,
	remotes: GitRemoteInfo[],
	ahead: number,
	behind: number,
	merging: boolean,
	hasAuthor: boolean,
	encrypted: boolean,
	unencryptedSecrets: boolean,
};

export type GitStatus = GitStatus_Serialize | GitStatus_Deserialize;

export type GitStatus_Deserialize = {
	files: GitFileChange_Deserialize[],
	conflicted: boolean,
};

export type GitStatus_Serialize = {
	files: GitFileChange_Serialize[],
	conflicted: boolean,
};

/**
 *  A gRPC call that reached the server but returned a non-OK status. Carries the
 *  partial timeline so the frontend can show what happened before the failure
 *  (mirrors `HttpFailure`).
 */
export type GrpcFailure = {
	message: string,
	events: TimelineEvent[],
};

/**
 *  A saved gRPC request — the gRPC counterpart of `HttpRequest`/`WsConnection`.
 *  Lives in the workspace tree as `grpc_{id}.yaml`. `message` is the request
 *  payload in protobuf-JSON (the form editor, MCP, and the dynamic codec all
 *  share this single representation). `metadata` rides the call as gRPC
 *  metadata (HTTP/2 headers); `auth` maps to metadata at send time.
 */
export type GrpcRequest = GrpcRequest_Serialize | GrpcRequest_Deserialize;

/**
 *  Editable fields of a gRPC request, bundled so the command stays within
 *  specta's 10-argument limit.
 */
export type GrpcRequestUpdate = GrpcRequestUpdate_Serialize | GrpcRequestUpdate_Deserialize;

/**
 *  Editable fields of a gRPC request, bundled so the command stays within
 *  specta's 10-argument limit.
 */
export type GrpcRequestUpdate_Deserialize = {
	target: string,
	tls: boolean,
	protoSource: ProtoSource,
	service: string | null,
	method: string | null,
	metadata: RequestParameter[],
	message: string,
	auth: AuthConfig_Deserialize,
};

/**
 *  Editable fields of a gRPC request, bundled so the command stays within
 *  specta's 10-argument limit.
 */
export type GrpcRequestUpdate_Serialize = {
	target: string,
	tls: boolean,
	protoSource: ProtoSource,
	service: string | null,
	method: string | null,
	metadata: RequestParameter[],
	message: string,
	auth: AuthConfig_Serialize,
};

/**
 *  A saved gRPC request — the gRPC counterpart of `HttpRequest`/`WsConnection`.
 *  Lives in the workspace tree as `grpc_{id}.yaml`. `message` is the request
 *  payload in protobuf-JSON (the form editor, MCP, and the dynamic codec all
 *  share this single representation). `metadata` rides the call as gRPC
 *  metadata (HTTP/2 headers); `auth` maps to metadata at send time.
 */
export type GrpcRequest_Deserialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	name: string,
	/**  `host:port` (no scheme). `tls` selects h2 over TLS vs plaintext h2c. */
	target: string,
	tls?: boolean,
	protoSource?: ProtoSource,
	service?: string | null,
	method?: string | null,
	metadata?: RequestParameter[],
	message?: string,
	auth?: AuthConfig_Deserialize,
	order?: number | null,
	createdAt: string,
	updatedAt: string,
};

/**
 *  A saved gRPC request — the gRPC counterpart of `HttpRequest`/`WsConnection`.
 *  Lives in the workspace tree as `grpc_{id}.yaml`. `message` is the request
 *  payload in protobuf-JSON (the form editor, MCP, and the dynamic codec all
 *  share this single representation). `metadata` rides the call as gRPC
 *  metadata (HTTP/2 headers); `auth` maps to metadata at send time.
 */
export type GrpcRequest_Serialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	name: string,
	/**  `host:port` (no scheme). `tls` selects h2 over TLS vs plaintext h2c. */
	target: string,
	tls: boolean,
	protoSource: ProtoSource,
	service?: string | null,
	method?: string | null,
	metadata?: RequestParameter[],
	message: string,
	auth: AuthConfig_Serialize,
	order: number | null,
	createdAt: string,
	updatedAt: string,
};

/**
 *  Result of a unary gRPC call. `status_code` is the gRPC status (0 = OK);
 *  `message` is the response payload in protobuf-JSON. `metadata`/`trailers`
 *  carry response headers and trailers.
 */
export type GrpcResponse = {
	requestId: string,
	statusCode: number,
	statusMessage: string,
	message?: string,
	metadata?: HttpResponseHeader[],
	trailers?: HttpResponseHeader[],
	totalMs: number | null,
	events?: TimelineEvent[],
	responseId?: string,
};

/**
 *  The four gRPC call shapes, derived from the method descriptor's
 *  client/server streaming flags.
 */
export type GrpcRpcKind = "unary" | "server_streaming" | "client_streaming" | "bidi";

/**
 *  One streaming-call transcript row. `data` is protobuf-JSON (mirrors
 *  `WsMessage`); `size` is its byte length.
 */
export type GrpcStreamMessage = {
	id: string,
	direction: WsDirection,
	data: string,
	size: number,
	at: string,
};

export type HttpFailure = {
	message: string,
	events: TimelineEvent[],
};

export type HttpRequest = HttpRequest_Serialize | HttpRequest_Deserialize;

export type HttpRequest_Deserialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	method: string,
	name: string,
	url: string,
	parameters?: RequestParameter[],
	headers?: RequestParameter[],
	body?: RequestBody_Deserialize | null,
	auth?: AuthConfig_Deserialize,
	order?: number | null,
	createdAt: string,
	updatedAt: string,
};

export type HttpRequest_Serialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	method: string,
	name: string,
	url: string,
	parameters?: RequestParameter[],
	headers?: RequestParameter[],
	body?: RequestBody_Serialize | null,
	auth: AuthConfig_Serialize,
	order: number | null,
	createdAt: string,
	updatedAt: string,
};

/**  Result of executing a saved `HttpRequest` in-app (not the raw HTTP `Response` type). */
export type HttpResponse = HttpResponse_Serialize | HttpResponse_Deserialize;

export type HttpResponseHeader = {
	name: string,
	value: string,
	atMs?: number | null,
};

/**  Result of executing a saved `HttpRequest` in-app (not the raw HTTP `Response` type). */
export type HttpResponse_Deserialize = {
	requestId: string,
	status: number,
	statusText: string,
	headers: HttpResponseHeader[],
	/**
	 *  UTF-8 body or lossy; non-text bodies are base64-encoded. Empty when
	 *  `body_windowed` — the body then lives in a side file fetched in windows.
	 */
	body: string,
	/**  Byte length of the response body (`u32` for JS number / specta interop; max ~4GiB). */
	bodySize: number,
	bodyIsText: boolean,
	/**
	 *  Large text bodies are stored out-of-line and read by line window via
	 *  `response_body_window`; `body` is empty and the frontend virtualizes.
	 */
	bodyWindowed?: boolean,
	/**  Line count of the stored (formatted) body — sizes the virtual viewport. */
	bodyLineCount?: number,
	/**  History id used to fetch windows/search; assigned on store, else empty. */
	responseId?: string,
	timing: HttpTiming,
	/**
	 *  Timeline event log; when empty the frontend reconstructs milestones
	 *  from `headers` + `timing`.
	 */
	events?: TimelineEvent[],
	/**  Present when redirects dropped the body or sensitive headers. */
	redirectWarning?: RedirectInfo | null,
	/**
	 *  Cookies from `Set-Cookie` (incl. redirect hops). Plaintext; the command
	 *  layer encrypts on write when the workspace is encrypted.
	 */
	capturedCookies?: StoredCookie_Deserialize[],
	/**
	 *  Cookies the executor sent (active jar, matched at send time). Aggregated
	 *  across hops, deduped by id.
	 */
	attachedCookies?: StoredCookie_Deserialize[],
	/**
	 *  Parsed frames for a `text/event-stream` response. Empty for normal
	 *  responses; the body stays empty when this is populated. Persisted with
	 *  the response so each run keeps its stream in history.
	 */
	sseFrames?: SseFrame_Deserialize[],
};

/**  Result of executing a saved `HttpRequest` in-app (not the raw HTTP `Response` type). */
export type HttpResponse_Serialize = {
	requestId: string,
	status: number,
	statusText: string,
	headers: HttpResponseHeader[],
	/**
	 *  UTF-8 body or lossy; non-text bodies are base64-encoded. Empty when
	 *  `body_windowed` — the body then lives in a side file fetched in windows.
	 */
	body: string,
	/**  Byte length of the response body (`u32` for JS number / specta interop; max ~4GiB). */
	bodySize: number,
	bodyIsText: boolean,
	/**
	 *  Large text bodies are stored out-of-line and read by line window via
	 *  `response_body_window`; `body` is empty and the frontend virtualizes.
	 */
	bodyWindowed: boolean,
	/**  Line count of the stored (formatted) body — sizes the virtual viewport. */
	bodyLineCount: number,
	/**  History id used to fetch windows/search; assigned on store, else empty. */
	responseId: string,
	timing: HttpTiming,
	/**
	 *  Timeline event log; when empty the frontend reconstructs milestones
	 *  from `headers` + `timing`.
	 */
	events: TimelineEvent[],
	/**  Present when redirects dropped the body or sensitive headers. */
	redirectWarning: RedirectInfo | null,
	/**
	 *  Cookies from `Set-Cookie` (incl. redirect hops). Plaintext; the command
	 *  layer encrypts on write when the workspace is encrypted.
	 */
	capturedCookies: StoredCookie_Serialize[],
	/**
	 *  Cookies the executor sent (active jar, matched at send time). Aggregated
	 *  across hops, deduped by id.
	 */
	attachedCookies: StoredCookie_Serialize[],
	/**
	 *  Parsed frames for a `text/event-stream` response. Empty for normal
	 *  responses; the body stays empty when this is populated. Persisted with
	 *  the response so each run keeps its stream in history.
	 */
	sseFrames?: SseFrame_Serialize[],
};

/**
 *  Phase timings (ms). DNS/TCP/TLS aren't available from the client → 0.0.
 *  `first_byte_ms` = start→headers; `download_ms` = headers→body read.
 */
export type HttpTiming = {
	dnsMs: number | null,
	connectMs: number | null,
	tlsMs: number | null,
	firstByteMs: number | null,
	downloadMs: number | null,
	totalMs: number | null,
};

/**
 *  Where a commit lands: a fresh workspace or an existing one (optionally under
 *  a parent folder).
 */
export type ImportDest = { kind: "new_workspace"; data: {
	name: string,
	encrypted: boolean,
} } | { kind: "existing_workspace"; data: {
	workspace_id: string,
	parent_folder_id: string | null,
} };

/**  The collection formats Voleeo can import. */
export type ImportFormat = "open_api" | "swagger2" | "postman" | "insomnia" | "bruno" | "yaak";

/**  One preview node the UI renders as a checkbox tree. */
export type ImportNode = ImportNode_Serialize | ImportNode_Deserialize;

/**  One preview node the UI renders as a checkbox tree. */
export type ImportNode_Deserialize = {
	id: string,
	/**  `"folder"` | `"request"`. */
	kind: string,
	name: string,
	/**  Request HTTP method (folders have none). */
	method: string | null,
	/**  Request source path (`/pet/{petId}`); folders have none. */
	path: string | null,
	/**  Folder/tag description; requests have none. */
	description: string | null,
	children: ImportNode_Deserialize[],
};

/**  One preview node the UI renders as a checkbox tree. */
export type ImportNode_Serialize = {
	id: string,
	/**  `"folder"` | `"request"`. */
	kind: string,
	name: string,
	/**  Request HTTP method (folders have none). */
	method?: string | null,
	/**  Request source path (`/pet/{petId}`); folders have none. */
	path?: string | null,
	/**  Folder/tag description; requests have none. */
	description?: string | null,
	children: ImportNode_Serialize[],
};

/**  What `import_preview` returns — a tree the UI shows with checkboxes. */
export type ImportPreview = ImportPreview_Serialize | ImportPreview_Deserialize;

/**  What `import_preview` returns — a tree the UI shows with checkboxes. */
export type ImportPreview_Deserialize = {
	format: ImportFormat,
	/**  Source spec version (e.g. `3.0.3`) when the format exposes one. */
	formatVersion: string | null,
	suggestedName: string,
	tree: ImportNode_Deserialize[],
	variableCount: number,
	warnings: string[],
};

/**  What `import_preview` returns — a tree the UI shows with checkboxes. */
export type ImportPreview_Serialize = {
	format: ImportFormat,
	/**  Source spec version (e.g. `3.0.3`) when the format exposes one. */
	formatVersion: string | null,
	suggestedName: string,
	tree: ImportNode_Serialize[],
	variableCount: number,
	warnings: string[],
};

/**  Result of a commit — counts + any warnings to surface. */
export type ImportSummary = {
	workspaceId: string,
	foldersCreated: number,
	requestsCreated: number,
	variablesCreated: number,
	warnings: string[],
};

/**  Which scope an `AuthConfig::Inherit` resolves against. */
export type InheritSource = 
/**  Nearest ancestor folder with an auth, else the workspace. Default. */
"folder" | 
/**  The workspace's own auth, skipping folders entirely. */
"workspace";

export type ItemKind = "request" | "folder" | "webSocket" | "grpc";

export type McpSettings = {
	enabled: boolean,
	token: string | null,
};

export type MoveItemUpdate = {
	id: string,
	kind: ItemKind,
	folderId: string | null,
	order: number | null,
};

/**
 *  Where OAuth 1.0 puts its `oauth_*` params: the `Authorization` header
 *  (default) or the URL query string.
 */
export type OAuth1Location = "header" | "query";

/**
 *  OAuth 1.0 signature method (RFC 5849 §3.4). `Hmac*` use the consumer/token
 *  secrets; `Rsa*` sign with the consumer's RSA private key; `PlainText` skips
 *  hashing and is only safe over TLS.
 */
export type OAuth1Signature = "hmac_sha1" | "hmac_sha256" | "hmac_sha512" | "rsa_sha1" | "rsa_sha256" | "rsa_sha512" | "plain_text";

/**
 *  How client credentials reach the token endpoint: HTTP Basic header (default)
 *  or in the request body.
 */
export type OAuth2ClientAuth = "basic_header" | "request_body";

/**
 *  OAuth 2.0 grant type (RFC 6749). `AuthorizationCode` is interactive (browser
 *  + loopback); the others fetch a token directly.
 */
export type OAuth2Grant = "client_credentials" | "authorization_code" | "password" | "implicit";

/**
 *  PKCE code-challenge method: SHA-256 (default, recommended) or plain (the
 *  verifier is sent as-is). Authorization-code grant only.
 */
export type OAuth2PkceMethod = "s256" | "plain";

/**
 *  The cached token's full fields, for the Auth-tab's expandable token view.
 *  Returns the raw access token (unmasked) so the user can copy it — only
 *  reached when they explicitly expand the panel. `None` when no token cached.
 */
export type Oauth2TokenDetails = {
	accessToken: string,
	tokenType: string,
	scope: string,
	expiresAt: number | null,
	refreshToken: string | null,
};

/**  Token state for the Auth-tab panel. Secrets are previewed, never returned. */
export type Oauth2TokenStatus = {
	hasToken: boolean,
	/**
	 *  Unix seconds; `None` = no token or no expiry. `f64` (not `i64`) because
	 *  specta forbids exporting BigInt-style types — lossless for real dates.
	 */
	expiresAt: number | null,
	scope: string,
	tokenPreview: string,
};

/**  Panel-size percentages for the API workspace layout. */
export type PanelSizes = {
	colPane1: number | null,
	colPane3: number | null,
	rowTree: number | null,
	rowInner: number | null,
};

/**  A protobuf enum member, for rendering an enum field as a select. */
export type ProtoEnumValue = {
	name: string,
	number: number,
};

/**
 *  One field of a message, flattened for form rendering. `oneof_group` ties
 *  mutually-exclusive fields together; `repeated`/`optional` drive widgets.
 */
export type ProtoFieldSchema = ProtoFieldSchema_Serialize | ProtoFieldSchema_Deserialize;

/**
 *  One field of a message, flattened for form rendering. `oneof_group` ties
 *  mutually-exclusive fields together; `repeated`/`optional` drive widgets.
 */
export type ProtoFieldSchema_Deserialize = {
	name: string,
	number: number,
	ty: ProtoFieldType_Deserialize,
	repeated: boolean,
	optional: boolean,
	oneofGroup?: string | null,
};

/**
 *  One field of a message, flattened for form rendering. `oneof_group` ties
 *  mutually-exclusive fields together; `repeated`/`optional` drive widgets.
 */
export type ProtoFieldSchema_Serialize = {
	name: string,
	number: number,
	ty: ProtoFieldType_Serialize,
	repeated: boolean,
	optional: boolean,
	oneofGroup?: string | null,
};

/**
 *  The type of a single message field — the discriminant the form renders from.
 *  `MessageRef` breaks recursion/depth: the frontend lazily expands it via
 *  `grpc_describe_message`. `Map` carries its key/value element types.
 */
export type ProtoFieldType = ProtoFieldType_Serialize | ProtoFieldType_Deserialize;

/**
 *  The type of a single message field — the discriminant the form renders from.
 *  `MessageRef` breaks recursion/depth: the frontend lazily expands it via
 *  `grpc_describe_message`. `Map` carries its key/value element types.
 */
export type ProtoFieldType_Deserialize = 
/**  Protobuf scalar; `name` is the proto type (`string`, `int32`, `bool`, …). */
({ kind: "scalar"; name: string }) & { key?: never; schema?: never; value?: never; values?: never } | ({ kind: "enum"; name: string; values: ProtoEnumValue[] }) & { key?: never; schema?: never; value?: never } | ({ kind: "message"; schema: ProtoMessageSchema_Deserialize }) & { key?: never; name?: never; value?: never; values?: never } | ({ kind: "message_ref"; name: string }) & { key?: never; schema?: never; value?: never; values?: never } | ({ kind: "map"; key: ProtoFieldType_Deserialize; value: ProtoFieldType_Deserialize }) & { name?: never; schema?: never; values?: never };

/**
 *  The type of a single message field — the discriminant the form renders from.
 *  `MessageRef` breaks recursion/depth: the frontend lazily expands it via
 *  `grpc_describe_message`. `Map` carries its key/value element types.
 */
export type ProtoFieldType_Serialize = 
/**  Protobuf scalar; `name` is the proto type (`string`, `int32`, `bool`, …). */
({ kind: "scalar"; name: string }) & { key?: never; schema?: never; value?: never; values?: never } | ({ kind: "enum"; name: string; values: ProtoEnumValue[] }) & { key?: never; schema?: never; value?: never } | ({ kind: "message"; schema: ProtoMessageSchema_Serialize }) & { key?: never; name?: never; value?: never; values?: never } | ({ kind: "message_ref"; name: string }) & { key?: never; schema?: never; value?: never; values?: never } | ({ kind: "map"; key: ProtoFieldType_Serialize; value: ProtoFieldType_Serialize }) & { name?: never; schema?: never; values?: never };

/**  A message's field set — the schema the generated form renders. */
export type ProtoMessageSchema = ProtoMessageSchema_Serialize | ProtoMessageSchema_Deserialize;

/**  A message's field set — the schema the generated form renders. */
export type ProtoMessageSchema_Deserialize = {
	name: string,
	fields: ProtoFieldSchema_Deserialize[],
};

/**  A message's field set — the schema the generated form renders. */
export type ProtoMessageSchema_Serialize = {
	name: string,
	fields: ProtoFieldSchema_Serialize[],
};

/**
 *  A method's I/O shape: the RPC kind plus the input message schema and the
 *  output message's full name (the form only needs to build the request).
 */
export type ProtoMethodInfo = ProtoMethodInfo_Serialize | ProtoMethodInfo_Deserialize;

/**
 *  A method's I/O shape: the RPC kind plus the input message schema and the
 *  output message's full name (the form only needs to build the request).
 */
export type ProtoMethodInfo_Deserialize = {
	name: string,
	fullName: string,
	kind: GrpcRpcKind,
	input: ProtoMessageSchema_Deserialize,
	outputName: string,
};

/**
 *  A method's I/O shape: the RPC kind plus the input message schema and the
 *  output message's full name (the form only needs to build the request).
 */
export type ProtoMethodInfo_Serialize = {
	name: string,
	fullName: string,
	kind: GrpcRpcKind,
	input: ProtoMessageSchema_Serialize,
	outputName: string,
};

/**  A service and its methods, returned by `grpc_list_services`. */
export type ProtoServiceInfo = ProtoServiceInfo_Serialize | ProtoServiceInfo_Deserialize;

/**  A service and its methods, returned by `grpc_list_services`. */
export type ProtoServiceInfo_Deserialize = {
	name: string,
	methods: ProtoMethodInfo_Deserialize[],
};

/**  A service and its methods, returned by `grpc_list_services`. */
export type ProtoServiceInfo_Serialize = {
	name: string,
	methods: ProtoMethodInfo_Serialize[],
};

/**
 *  Where a gRPC request's protobuf schema comes from. `Reflection` queries the
 *  server's reflection service at send time; `Files` compiles local `.proto`s
 *  (absolute paths) — files are referenced, never copied into the workspace.
 */
export type ProtoSource = { kind: "reflection" } | { kind: "files"; paths: string[]; include_dirs?: string[] };

/**
 *  Warning surfaced when following redirects silently dropped part of the
 *  request, so the result isn't mistaken for a server-side problem.
 */
export type RedirectInfo = {
	hopCount: number,
	/**  Body dropped when a 301/302/303 downgraded a non-GET request. */
	bodyDropped: boolean,
	/**  Sensitive headers stripped on a cross-origin redirect. */
	droppedHeaders: string[],
};

/**
 *  `text` carries raw bodies (json/xml/text/html). `fields` carries
 *  form-urlencoded / multipart entries. `file_path` carries the binary upload.
 *  New fields are `Option` so existing `{ kind, text }` payloads stay valid.
 */
export type RequestBody = RequestBody_Serialize | RequestBody_Deserialize;

/**
 *  `text` carries raw bodies (json/xml/text/html). `fields` carries
 *  form-urlencoded / multipart entries. `file_path` carries the binary upload.
 *  New fields are `Option` so existing `{ kind, text }` payloads stay valid.
 */
export type RequestBody_Deserialize = {
	kind: BodyKind,
	text?: string,
	fields?: BodyField_Deserialize[] | null,
	filePath?: string | null,
	contentType?: string | null,
	/**  GraphQL variables (JSON object string). Only meaningful for `Graphql`. */
	graphqlVariables?: string | null,
};

/**
 *  `text` carries raw bodies (json/xml/text/html). `fields` carries
 *  form-urlencoded / multipart entries. `file_path` carries the binary upload.
 *  New fields are `Option` so existing `{ kind, text }` payloads stay valid.
 */
export type RequestBody_Serialize = {
	kind: BodyKind,
	text: string,
	fields?: BodyField_Serialize[] | null,
	filePath?: string | null,
	contentType?: string | null,
	/**  GraphQL variables (JSON object string). Only meaningful for `Graphql`. */
	graphqlVariables?: string | null,
};

export type RequestParameter = {
	id: string,
	name: string,
	value: string,
	enabled: boolean,
};

export type SameSite = "strict" | "lax" | "none";

export type SearchOpts = {
	caseSensitive?: boolean,
	wholeWord?: boolean,
};

/**
 *  Frontend-resolved send-time overrides. Bundled into one struct because
 *  `tauri_specta` caps command arity at 10 args. `cookie_overrides` /
 *  `auth_override` carry values only JS can resolve (`{{ uuid.v4() }}`, dynamic
 *  signing config); when present the backend skips its own resolution for them.
 */
export type SendOverrides = SendOverrides_Serialize | SendOverrides_Deserialize;

/**
 *  Frontend-resolved send-time overrides. Bundled into one struct because
 *  `tauri_specta` caps command arity at 10 args. `cookie_overrides` /
 *  `auth_override` carry values only JS can resolve (`{{ uuid.v4() }}`, dynamic
 *  signing config); when present the backend skips its own resolution for them.
 */
export type SendOverrides_Deserialize = {
	url: string | null,
	body: RequestBody_Deserialize | null,
	headers: RequestParameter[] | null,
	calledFrom: string | null,
	resolutionNotes: string[] | null,
	environmentId: string | null,
	cookieOverrides: StoredCookie_Deserialize[] | null,
	authOverride: AuthConfig_Deserialize | null,
};

/**
 *  Frontend-resolved send-time overrides. Bundled into one struct because
 *  `tauri_specta` caps command arity at 10 args. `cookie_overrides` /
 *  `auth_override` carry values only JS can resolve (`{{ uuid.v4() }}`, dynamic
 *  signing config); when present the backend skips its own resolution for them.
 */
export type SendOverrides_Serialize = {
	url: string | null,
	body: RequestBody_Serialize | null,
	headers: RequestParameter[] | null,
	calledFrom: string | null,
	resolutionNotes: string[] | null,
	environmentId: string | null,
	cookieOverrides: StoredCookie_Serialize[] | null,
	authOverride: AuthConfig_Serialize | null,
};

/**
 *  Signed contributions for preview / "Copy as …": header params and/or query
 *  params (OAuth 1.0 can place its params in either).
 */
export type SignedAuthParts = {
	headers: RequestParameter[],
	query: RequestParameter[],
};

/**
 *  One frame parsed from a `text/event-stream` response and pushed to the UI
 *  live. `seq` is the 0-based arrival order within a send (the React key);
 *  `data` joins multiple `data:` lines with `\n`. Omitted fields were absent in
 *  the frame.
 */
export type SseFrame = SseFrame_Serialize | SseFrame_Deserialize;

/**
 *  One frame parsed from a `text/event-stream` response and pushed to the UI
 *  live. `seq` is the 0-based arrival order within a send (the React key);
 *  `data` joins multiple `data:` lines with `\n`. Omitted fields were absent in
 *  the frame.
 */
export type SseFrame_Deserialize = {
	seq: number,
	event?: string | null,
	data: string,
	lastEventId?: string | null,
	/**  SSE reconnect hint (ms). `u32` keeps it JS-number / specta safe. */
	retry?: number | null,
	atMs: number | null,
};

/**
 *  One frame parsed from a `text/event-stream` response and pushed to the UI
 *  live. `seq` is the 0-based arrival order within a send (the React key);
 *  `data` joins multiple `data:` lines with `\n`. Omitted fields were absent in
 *  the frame.
 */
export type SseFrame_Serialize = {
	seq: number,
	event?: string | null,
	data: string,
	lastEventId?: string | null,
	/**  SSE reconnect hint (ms). `u32` keeps it JS-number / specta safe. */
	retry?: number | null,
	atMs: number | null,
};

/**
 *  A single cookie. `value` is plaintext over IPC; the Tauri command layer
 *  transforms it to ciphertext when `value_encrypted` is true and the workspace
 *  is encrypted (mirrors `transform_auth_secrets` for request auth).
 * 
 *  `(domain, path, name)` is the uniqueness key per RFC 6265 §5.3.
 */
export type StoredCookie = StoredCookie_Serialize | StoredCookie_Deserialize;

/**
 *  A single cookie. `value` is plaintext over IPC; the Tauri command layer
 *  transforms it to ciphertext when `value_encrypted` is true and the workspace
 *  is encrypted (mirrors `transform_auth_secrets` for request auth).
 * 
 *  `(domain, path, name)` is the uniqueness key per RFC 6265 §5.3.
 */
export type StoredCookie_Deserialize = {
	id: string,
	domain: string,
	/**
	 *  When true, only the exact `domain` matches (cookie was set without an
	 *  explicit `Domain=` attribute). When false, subdomains also match.
	 */
	hostOnly?: boolean,
	path: string,
	name: string,
	value: string,
	valueEncrypted?: boolean,
	secure?: boolean,
	httpOnly?: boolean,
	sameSite?: SameSite | null,
	/**
	 *  RFC 3339 UTC. `None` ⇒ session cookie (kept across app restarts here —
	 *  users want to keep sessions around between sends).
	 */
	expires?: string | null,
	createdAt: string,
	updatedAt: string,
};

/**
 *  A single cookie. `value` is plaintext over IPC; the Tauri command layer
 *  transforms it to ciphertext when `value_encrypted` is true and the workspace
 *  is encrypted (mirrors `transform_auth_secrets` for request auth).
 * 
 *  `(domain, path, name)` is the uniqueness key per RFC 6265 §5.3.
 */
export type StoredCookie_Serialize = {
	id: string,
	domain: string,
	/**
	 *  When true, only the exact `domain` matches (cookie was set without an
	 *  explicit `Domain=` attribute). When false, subdomains also match.
	 */
	hostOnly: boolean,
	path: string,
	name: string,
	value: string,
	valueEncrypted: boolean,
	secure: boolean,
	httpOnly: boolean,
	sameSite?: SameSite | null,
	/**
	 *  RFC 3339 UTC. `None` ⇒ session cookie (kept across app restarts here —
	 *  users want to keep sessions around between sends).
	 */
	expires?: string | null,
	createdAt: string,
	updatedAt: string,
};

export type StoredGrpcResponseSummary = {
	id: string,
	requestId: string,
	recordedAt: string,
	statusCode: number,
	statusMessage: string,
	totalMs: number | null,
};

/**
 *  One streaming session: every message + lifecycle event between a stream start
 *  and its close.
 */
export type StoredGrpcSession = {
	id: string,
	requestId?: string,
	recordedAt?: string,
	messages?: GrpcStreamMessage[],
	events?: TimelineEvent[],
};

export type StoredGrpcSessionSummary = {
	id: string,
	recordedAt: string,
	messageCount: number,
	eventCount: number,
};

/**  Persisted HTTP response entry (machine-local, never synced). */
export type StoredHttpResponse = StoredHttpResponse_Serialize | StoredHttpResponse_Deserialize;

/**  Lightweight summary returned by `response_list` — avoids loading full bodies. */
export type StoredHttpResponseSummary = {
	id: string,
	requestId: string,
	recordedAt: string,
	status: number,
	statusText: string,
	bodySize: number,
	totalMs: number | null,
};

/**  Persisted HTTP response entry (machine-local, never synced). */
export type StoredHttpResponse_Deserialize = {
	id: string,
	workspaceId: string,
	requestId: string,
	recordedAt: string,
	response: HttpResponse_Deserialize,
};

/**  Persisted HTTP response entry (machine-local, never synced). */
export type StoredHttpResponse_Serialize = {
	id: string,
	workspaceId: string,
	requestId: string,
	recordedAt: string,
	response: HttpResponse_Serialize,
};

/**  One connection session: everything exchanged between a connect and its close. */
export type StoredWsSession = {
	id: string,
	connectionId?: string,
	recordedAt?: string,
	messages?: WsMessage[],
	events?: TimelineEvent[],
};

/**  Lightweight session summary for the history picker. */
export type StoredWsSessionSummary = {
	id: string,
	recordedAt: string,
	messageCount: number,
	eventCount: number,
};

/**
 *  One Timeline-tab row; `at_ms` is elapsed from request start. `kind` is a
 *  string (not an enum) so the executor can add types without breaking codegen.
 *  Today: `config` | `info` | `send` | `recv` | `dns` | `chunk` | `done`.
 */
export type TimelineEvent = {
	atMs: number | null,
	kind: string,
	text: string,
};

/**
 *  Read-only summary of a bundle for the import confirm screen — what each
 *  workspace would restore — without writing anything.
 */
export type VoleeoBundlePreview = VoleeoBundlePreview_Serialize | VoleeoBundlePreview_Deserialize;

/**
 *  Read-only summary of a bundle for the import confirm screen — what each
 *  workspace would restore — without writing anything.
 */
export type VoleeoBundlePreview_Deserialize = {
	workspaces: BundleWorkspacePreview_Deserialize[],
};

/**
 *  Read-only summary of a bundle for the import confirm screen — what each
 *  workspace would restore — without writing anything.
 */
export type VoleeoBundlePreview_Serialize = {
	workspaces: BundleWorkspacePreview_Serialize[],
};

export type VoleeoError = { kind: "storage"; data: string } | { kind: "http"; data: string } | { kind: "http_failed"; data: HttpFailure } | { kind: "cancelled" } | { kind: "not_found"; data: string } | { kind: "invalid_config"; data: string } | { kind: "crypto"; data: string } | { kind: "git"; data: string } | { kind: "web_socket"; data: string } | { kind: "web_socket_closed" } | { kind: "grpc"; data: string } | { kind: "grpc_failed"; data: GrpcFailure } | { kind: "import"; data: string };

/**  Saved window dimensions (logical pixels). */
export type WindowSize = {
	width: number | null,
	height: number | null,
};

export type Workspace = Workspace_Serialize | Workspace_Deserialize;

/**
 *  All UI/local settings for a single workspace.
 *  Extend this struct as new per-workspace settings are added.
 */
export type WorkspaceSettings = WorkspaceSettings_Serialize | WorkspaceSettings_Deserialize;

/**
 *  All UI/local settings for a single workspace.
 *  Extend this struct as new per-workspace settings are added.
 */
export type WorkspaceSettings_Deserialize = {
	panelSizes?: PanelSizes | null,
	openedAt?: string | null,
	panelLayout?: string | null,
	windowSize?: WindowSize | null,
	activeEnvId?: string | null,
	itemsCount?: number | null,
	treeVisible?: boolean | null,
	editorVisible?: boolean | null,
};

/**
 *  All UI/local settings for a single workspace.
 *  Extend this struct as new per-workspace settings are added.
 */
export type WorkspaceSettings_Serialize = {
	panelSizes?: PanelSizes | null,
	openedAt?: string | null,
	panelLayout?: string | null,
	windowSize?: WindowSize | null,
	activeEnvId?: string | null,
	itemsCount?: number | null,
	treeVisible?: boolean | null,
	editorVisible?: boolean | null,
};

export type Workspace_Deserialize = {
	id: string,
	name: string,
	model: string,
	encrypted?: boolean,
	syncDir?: string | null,
	keyCheck?: string | null,
	/**  Merged into every request; folder/request headers override (case-insensitive). */
	headers?: RequestParameter[],
	/**  Fallback auth for `AuthConfig::Inherit` when no ancestor folder defines one. */
	auth?: AuthConfig_Deserialize,
	/**
	 *  Per-workspace DNS overrides — resolved at send time, scoped to requests
	 *  from this workspace (like `/etc/hosts` but local to Voleeo).
	 */
	dnsOverrides?: DnsOverride[],
	createdAt: string,
	updatedAt: string,
};

export type Workspace_Serialize = {
	id: string,
	name: string,
	model: string,
	encrypted: boolean,
	syncDir?: string | null,
	keyCheck?: string | null,
	/**  Merged into every request; folder/request headers override (case-insensitive). */
	headers?: RequestParameter[],
	/**  Fallback auth for `AuthConfig::Inherit` when no ancestor folder defines one. */
	auth?: AuthConfig_Serialize,
	/**
	 *  Per-workspace DNS overrides — resolved at send time, scoped to requests
	 *  from this workspace (like `/etc/hosts` but local to Voleeo).
	 */
	dnsOverrides?: DnsOverride[],
	createdAt: string,
	updatedAt: string,
};

/**
 *  A saved WebSocket connection — the WS counterpart of `HttpRequest`. Lives in
 *  the same workspace tree (`ws_{id}.yaml`); `auth` + `headers` ride the
 *  handshake; `parameters` mirror HTTP path/query params (`:name` segments in
 *  the URL bind to `parameters` by name; the rest become the query string).
 */
export type WsConnection = WsConnection_Serialize | WsConnection_Deserialize;

/**
 *  A saved WebSocket connection — the WS counterpart of `HttpRequest`. Lives in
 *  the same workspace tree (`ws_{id}.yaml`); `auth` + `headers` ride the
 *  handshake; `parameters` mirror HTTP path/query params (`:name` segments in
 *  the URL bind to `parameters` by name; the rest become the query string).
 */
export type WsConnection_Deserialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	name: string,
	url: string,
	parameters?: RequestParameter[],
	headers?: RequestParameter[],
	auth?: AuthConfig_Deserialize,
	order?: number | null,
	createdAt: string,
	updatedAt: string,
};

/**
 *  A saved WebSocket connection — the WS counterpart of `HttpRequest`. Lives in
 *  the same workspace tree (`ws_{id}.yaml`); `auth` + `headers` ride the
 *  handshake; `parameters` mirror HTTP path/query params (`:name` segments in
 *  the URL bind to `parameters` by name; the rest become the query string).
 */
export type WsConnection_Serialize = {
	id: string,
	type: string,
	model: string,
	workspaceId: string,
	folderId: string | null,
	name: string,
	url: string,
	parameters?: RequestParameter[],
	headers?: RequestParameter[],
	auth: AuthConfig_Serialize,
	order: number | null,
	createdAt: string,
	updatedAt: string,
};

/**  Which way a transcript message traveled. */
export type WsDirection = "outgoing" | "incoming";

/**
 *  One transcript row. `size` is the byte length of the decoded payload (`u32`
 *  for JS-number/specta interop). `at` is a UTC ISO-8601 timestamp.
 */
export type WsMessage = {
	id: string,
	direction: WsDirection,
	kind: WsMessageKind,
	data: string,
	size: number,
	at: string,
};

/**
 *  A WebSocket message payload encoding. `Text` carries UTF-8; `Binary`
 *  carries base64 (mirrors `HttpResponse.body`'s non-text convention).
 */
export type WsMessageKind = "text" | "binary";

/* Tauri Specta runtime */
async function typedError<T, E>(result: Promise<T>): Promise<{ status: "ok"; data: T } | { status: "error"; error: E }> {
    try {
        return { status: "ok", data: await result };
    } catch (e) {
        if (e instanceof Error) throw e;
        return { status: "error", error: e as any };
    }
}

